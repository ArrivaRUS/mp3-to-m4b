# HEARTBEAT — mp3-to-m4b

> «Где мы сейчас». Юрка обновляет в конце сессии. Память живёт на диске, не в голове.
>
> **Обновлено:** 2026-06-30

## Что это
macOS-утилита: слежение за выбранной папкой → сборка mp3 в аудиокнигу **.m4b**
(главы + обложка), с **обязательным подтверждением** качества и обложки во
всплывающем окне ПЕРЕД конвертацией. Функциональный клон каркаса соседа
**fb2-to-epub** (нативное SwiftUI-приложение + фоновый launchd-агент + `state.json`),
движок fb2→epub заменён на mp3→m4b.

## Фаза
ФАЗА 4 (разработка). Дизайн ✅ (G3) · **архитектура ✅ (G4 принят)** — синтез `arch/synthesis.md`.
**M0 ✅ · M0.5 ✅ · M1-вертикаль ✅** — реальная разведка (ffprobe) и реальная сборка `.m4b` доказаны (selfcheck M0 36/36 · M0.5 23/23 · M1 34/34; Юрка вскрыл боевую книгу: 5 глав 1/1000, обложка mjpeg attached_pic, brand M4A, +faststart, 44100/стерео, без дрейфа). **Окно §3 ✅** (4 состояния сверены вживую по window-id; кнопки converting/error отложены — D13). **Цепочка обложки ✅** (движок Pillow+веб + пикер в окне + выбор едет в `confirm-build` params → `build_m4b` вшивает ИМЕННО выбранную; `selfcheck_cover_pick` 16/16 pixel-доказал; Юрка сверил пикер вживую). **Группировка ✅** (loose-mp3 в корне → sheet D1 → `grouping-choice` → агент материализует combine→1 / separate→N книг; `selfcheck_grouping` 22/22; Юрка поймал баг наложения чипов вживую → 2 фикса, урок `.patches/002`: вёрстку сверять глазами, SwiftUI-флоу не самосайзится по высоте). **Очередь (display) ✅** (4 секции по статусам + batch-чип + навигация «Позже в очередь»; `selfcheck_queue` 18/18; Юрка self-check'ом вскрыл баг — диспетчер не обновлял витрину при `converting` → фикс `scan.refresh_showcase()` на всех переходах; регрессии зелёные). **Status (spec §5) ✅** (hero+кольцо c `totals.built`, 3 стат-карты, агент проецирует `totals.built/today`+`engine`; `selfcheck_status` 21/21; «глава X из Y» нет → «Сейчас:<книга>»+общий прогресс; наезд «последних книг» на футер ПОЧИНЕН — список height-capped+скролл над футером, Юрка сверил на 6 книгах; долг: тот же паттерн в `QueueView` — чинить в слое cancel). **⚡ Self-check'и развязаны ✅** — вложенные регрессии (~30 мин) убраны, плоский `selfcheck_all` за **~2 мин** (Юрка подтвердил). **Отмена сборки (D13) ✅** — кооперативная (Popen+опрос, агент гасит свой ffmpeg, без kill по pid; converting→pending-confirm; danger-кнопка в QueueView + наезд QueueView починен; `selfcheck_cancel` 16/16 — нет осиротыша/частичного файла; `selfcheck_all` **9/9**; Юрка сверил вживую на 9 книгах + cancel-команда корректной формы). **Надёжность (E1–E18) ✅** — аудит (12 уже-обработанных) + фиксы 3 пробелов: E3 битый→вся книга в error (решение человека), E5 нет места→`no_space` danger+cleanup до ffmpeg, E10 недокопированный→дебаунс в скане; E15 порог<главы→DEFER до нарезки. `selfcheck_reliability` 16/16, полный гейт `selfcheck_all` **10/10** (216s, Юрка подтвердил). **Нарезка (P1) ✅** — движок `split.py` (plan_parts+split stream-copy §3, `-map_chapters 1`, oversize E15) + UI-панель (слайдер 100–700МБ + превью «≈N частей»+чипы, `SplitPlanner` порт plan_parts); `selfcheck_split` 26/26, Юрка вскрыл части ffprobe (без дублей глав, stream-copy, обложка/title) + сверил панель вживую; `selfcheck_all` **11/11**. **Упаковка ✅** — `packaging/installer.sh` (ffmpeg-детект, venv+Pillow, launchd WatchPaths=папка+commands, plutil) + `build/build-dmg.sh`; **Setup-экран §01 ✅** (7-й из 7: ffmpeg-статус+выбор папки+«Установить»→запуск забандленного installer.sh; детект первого запуска; Юрка сверил вживую). **DMG `build/dist/mp3-to-m4b-0.1.0.dmg`** (3.4МБ, ad-hoc) ставится через окно без терминала; Юрка проверил безопасно (verify/монтаж/dry-run, систему не трогал). **🎉 M1 ПОЛНОСТЬЮ ЗАВЕРШЁН — все 7 экранов + функции + GUI-установка; `selfcheck_all` 11/11.** Осталось до релиза: **QA человеком на реальных книгах** (поставить DMG → Apple Books, test-plan §QA) · код-ревью (CodeReviewer∥Codex) → ⛔ **G5** (PR/релиз, СТОП — нужно «да»).

## Сделано (2026-06-28)
- Интейк: папка-сосед создана, git init на `main`, репо `ArrivaRUS/mp3-to-m4b` (публичный).
- Скоуп-гейт пройден — 3 решения человека (D1–D3) + ядро ТЗ (D4–D6). См. `decisions/log.md`.
- Тех-разведка ✅ — `research/m4b-toolchain.md`. **Вывод: одного `ffmpeg`(+`ffprobe`) достаточно** (склейка→AAC concat-filter, главы FFMETADATA `1/1000`, обложка `attached_pic`, контейнер `-f ipod +faststart`, нарезка stream-copy + обязательный `-map_chapters`). Доп. зависимости не нужны; запасные обложки — cairosvg/pillow (квадрат 1:1). Грабли: raw-mp3 в m4b нельзя (только AAC); `libfdk` недоступен → встроенный `aac`.
- PRD / истории / беклог ✅ (`prd/`). **G1 принят (2026-06-28):** цепочка обложки и Status — в MVP (D7, D8); нарезка — v1.1. MVP = 26 Must.
- Логотип ✅ **G2a (2026-06-28):** направление **02 «книга + play»**, единое семейство с fb2, цвет cyan→teal→indigo (D9). Финал + лого-пак готовы (`branding/logo.svg`, `icon-app/small/mono/menubar`, `brand-basics.md`).
- Макеты ✅ **G2b принят (2026-06-28):** 7 экранов + флоу (`design/`). Правки приёмки: компактный блок качества + главы на всю высоту окна подтверждения (D10).
- Токены ✅ (`design/tokens.*`, ~210) + **дизайн-спец ✅ G3 (2026-06-28, D11)** — `design/spec.md` + референсы `design/refs/`. Правила реализации (cap высоты окна, `.contentShape`, имена под `Tokens.swift`) зафиксированы.
- Двойной архитектор ✅ (`arch/plan-claude.md` ∥ `arch/plan-codex.md`) + **синтез ✅ G4 (2026-06-28, D12)** — `arch/synthesis.md`. Каркас-клон fb2; агент-единственный-писатель + command-log/манифесты; Python-движок; обложки Pillow.

## Следующий шаг
1. **M0 ✅ закоммичен** (протокол-скелет, 6/6, доказан вживую). app/agent/bin/build в репо.
2. **M0.5** — реальная разведка: `probe.py`/`metadata.py` через **ffprobe** (длительности глав, ID3 автор/название, детект встроенной обложки) → манифест с реальными данными + длительностями в окне.
3. **M1** — `build_m4b.py` (ffmpeg → реальный `.m4b` с главами+обложкой) → окно подтверждения целиком → цепочка обложки (mp3→веб→Pillow) → группировка → очередь → Status → надёжность → нарезка (P1) → упаковка/QA → ⛔ **G5** (PR/релиз).
4. Ревью (CodeReviewer ∥ Codex) → тесты (Tester) → QA на каждом крупном куске.

## Принятые тех-дефолты (ратифицировать на G4)
- Кодек: встроенный `aac` (QA опц. сравнит `aac_at`). `stik=Audiobook` — НЕ в v1 (QA проверит распознавание в Apple Books; иначе добавить опц. AtomicParsley).
- Параметры по умолчанию: 192 kbps стерео, 44100 Гц. Нарезка — по умолчанию выкл.
- Обложки: из mp3 → веб (DuckDuckGo/Yandex «автор+название+аудиокнига» + litres + OpenLibrary) → генерация квадратных вариантов.

## Карта (откуда что брать)
- **Прообраз архитектуры:** `../2026.06 fb2-to-epub/` — `app/*.swift`, `bin/*`,
  `launchd/*`, `packaging/installer.sh`, `build/*`, `arch/*`, `.patches/*`.
- **Тех-разведка движка:** `research/m4b-toolchain.md`.
- **Решения:** `decisions/log.md`. **Уроки:** `.patches/NNN-*.md`.

## Счётчики (anti-drift)
- `delegations`: 38 (researcher; product-manager; brand-designer ×2; ux-analyst ×3; design-token-extractor; architect-claude; architect-codex; developer ×28 — M0.1–M0.6, M0.5-разведка, M1-вертикаль, окно-§3, обложка-движок, группировка, +2 фикса чипов, очередь-display, фикс converting-витрины, Status-каркас, фикс selfcheck_status, развязка self-check'ов, фикс раскладки Status, отмена-механика, отмена-UI+QueueView-фикс, надёжность-аудит, надёжность-фиксы, доводка-надёжности, нарезка-движок, нарезка-UI, упаковка-installer+DMG, Setup-экран-§01). NB: обложка-пикер НЕ делегировал (код был на диске, верифицировал). Несколько фоновых агентов оборвались по ПОТЕРЕ СВЯЗИ (закрытый ноут / 403 auth), НЕ по отмене человеком — каждый раз код приезжал на диск, Юрка сам прогонял self-check и чинил (урок памяти `interrupted-agent-not-user-cancel`).
- `yurka_direct_actions`: 3 — **срыв в ручной режим:** лепил макеты сам в чат-виджете на G2b → урок `.patches/001`. Впредь: правки макета через UXAnalyst, показ — только реальным рендером файла.
