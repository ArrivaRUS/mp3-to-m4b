# HEARTBEAT — mp3-to-m4b

> «Где мы сейчас». Юрка обновляет в конце сессии. Память живёт на диске, не в голове.
>
> **Обновлено:** 2026-07-25

---

## ⚡ АКТИВНАЯ ЗАДАЧА (2026-07-26, ~14:00): релиз 1.0 — ВСЯ РАЗРАБОТКА M0–M6 ЗАКРЫТА И ЗАКОММИЧЕНА. ИДЁТ M7 (РЕЛИЗ)

### ГДЕ МЫ ТОЧНО (для продолжения после компактификации)
- **Коммиты:** `60b0450` (M1–M6, 30 файлов) + `ffbeb36` (M0) — ЛОКАЛЬНЫЕ, не запушены.
  origin/main = `236e9a7` (фиксы+планы уже на GitHub). Пуш двух коммитов — только по явному «да».
- **Гейт:** selfcheck_all **20/20 сюит**, blast_radius чист. selfcheck_app_routing 205/205.
- **Живая сверка Юрки сделана:** карточка blocked (окно росло 572→861 и сжималось обратно),
  янтарный подвал «Агент запущен, но папку не читает», FDA-раскрывашка, «Пропустить»/skip-цикл,
  пресет «ко всем», клэмпы окна. Всё вживую, скриншотами.
- **Заметки к релизу:** `docs/release-notes-1.0.md` — готовы, вычитаны (humanizer пройден).
- **Боевая система ЧИСТАЯ:** mp3 НЕ установлен (нет App Support, plist, job, лога);
  `~/Desktop/mp3-to-m4b` пуста. fb2-сосед работает, не задет.

### ✅ M7-ПРОГРЕСС (2026-07-26, 14:0x): БОЕВАЯ УСТАНОВКА 1.0 СДЕЛАНА И ПРОВЕРЕНА
- Версия 1.0 везде, коммит `2676973`. Сборка: все 4 стража зелёные, DMG
  `build/dist/mp3-to-m4b-1.0.dmg` (4.6M, sha256 `3114023e…`), гейт 20/20 FAIL:0.
- **Установлено в /Applications** (helper sha сходится), человек нажал «Разрешить» —
  запись TCC `DesktopFolder|2` на боевой helper. receipt: mode full, engine 1.0,
  gen `CD41EB40…`, first_access_ts записан; launchd PA0 = helper (1 элемент);
  folder_access = **ok**, поколения совпали.
- **Живой e2e НА БОЕВОЙ системе:** дроп книги → окно само всплыло → «Собрать» →
  .m4b AAC 192k/стерео/3 главы. Счётчики «1 собрано / 1 за сегодня». ConsentNotice-
  оверлей и честный setupStep отработали вживую. Тестовые артефакты убраны, папка пуста.

### ✅✅ РЕБУТ-ТЕСТ №2 ПРОЙДЕН (2026-07-28, 00:0x) — ЧИТАТЬ ПЕРВЫМ

Человек поставил новый образ, перезагрузился (23:56), положил книгу. **Окно всплыло само.**

**Трасса (`/usr/bin/log`, pid 1646):**
| время | событие |
|---|---|
| 23:58:19.951 | `agent_started` — RunAtLoad после ребута |
| 23:58:25.043 | `agent_started` — WatchPaths на дроп (+5 с = ThrottleInterval) |
| 23:58:36.820 | `app_nudged` — **11.8 с работы агента** (56 mp3: ID3 + длительности) |
| 23:58:36.801 | LaunchServices: Launch request, pid 1646 |
| 23:58:36.889 | WindowServer: `CPS: … presents 0 windows` ← отказ №1 |
| 23:58:37.006 | WindowServer: `CPS: Rejecting … activation count` ← отказ №2 |
| **23:58:37.164** | **`FUSBProcessWindowState: visible` — ОКНО ВИДНО** |
| 23:58:42.855 | `occluded` — человек сам переключился в другое окно |

**Ключевой факт: ОС отказала в активации ровно теми же двумя строками, что и в провальном
прогоне 21:13 — и окно всё равно стало видимым.** Было 85 с (и только по клику человека),
стало **0.36 с** от запуска. Приложение живо, App Nap не включался, самозавершения нет.

⚠️ **ЧЕСТНАЯ ГРАНИЦА: лестница эскалации в этом прогоне НЕ СРАБОТАЛА.** Окно стало видимым за
0.36 с — раньше первой самопроверки (1.0 с). Значит хватило базового пути
(`makeKeyAndOrderFront` + `activate`), потому что сразу после ребута экран был пустой и накрывать
окно было нечем. **Тяжёлый случай — чужое окно/полноэкранное приложение поверх — живьём НЕ
проверен**, `orderFrontRegardless()` и подскок дока подтверждены только юнит-тестами.
Повторить тест с активным чужим окном на переднем плане.

ℹ️ **Ощущаемые человеком «10 секунд» — это АГЕНТ, а не окно.** Но не чтение метаданных, как я
сначала сказал: замер показал **48 % — поиск обложки в интернете** (14 HTTP подряд) и **34 % —
112 запусков ffprobe там, где нужно 56** (двойной проб). Урок: `.patches/007`.

### ⚡ ОПТИМИЗАЦИЯ ВНЕДРЕНА (2026-07-28, ~00:5x) — БЕЗ СМЕНЫ ПОВЕДЕНИЯ
`agent/scan.py` + `agent/cover.py`: убран двойной проб (`_write_manifest` берёт колбэк `derive`,
проб один и НИЖЕ короткого замыкания по `source_rev`) · `_probe_book` параллельный (8 воркеров,
порядок сохранён) · кандидаты обложек волнами · `search_web` ∥ `generate_variants`.

| измерение | было | стало |
|---|---|---|
| `run_scan` (медиана 7 прогонов) | 9.40 с | **3.05 с** (−67.5 %) |
| весь процесс `--scan` | 9.19 с | 3.30 с (−64 %) |
| **холостой тик раз в 300 с** | 1.50 с / **56** ffprobe | **0.002 с / 0** (−99.9 %) |
| `selfcheck_all` | 240.7 с | 199.3 с |

Поведение доказано неизменным: дампы `state.json` + всех манифестов + имён файлов обложек до/после
**идентичны** (маскированы только случайные `confirm_token`/`ts`/`token`). Гейт **21/21, три
прогона** (внесены потоки ⇒ флак = красное, `.patches/005` §9). Оценка боевого времени человека:
**~4–5 с** вместо 11.8 с.

### 🏗 D17 РАННИЙ НУДЖ — M-A/M-B/M-C/M-E ГОТОВЫ, ИДЁТ M-D (тесты)

**Арх-гейт пройден:** два архитектора (`arch/early-nudge-claude.md` ∥ `arch/early-nudge-codex.md`
+ сырая трасса Sol), суд и синтез Юрки → **`arch/early-nudge-synthesis.md` — ИСТОЧНИК ПРАВДЫ**.
Основа от #1 (веб вне критического пути + непустой скелет + фазы), привито от #2 (отдельный
`build_token`, дыра в `_files_are_stable`, таймаут ≠ blocked, второй подъём окна, generation-scoped
имена обложек, порядок публикации).

**Протокол (реализован):** `phase: skeleton → chapters → ready → done`, **нет поля = `done`**
(совместимость без миграции). `confirm_token` чеканится ОДИН раз на скелете ⇒ ключ леджера у
скелета == у ready ⇒ **две публикации = один нудж**. `build_token` (32 hex) создаётся ТОЛЬКО в
`_finish_manifest` и является пропуском к сборке; приложение обязано вернуть его эхом.

**ЗАМЕРЫ (56 глав, боевой `--scan`):**
| режим | до окна | до активной «Собрать» |
|---|---|---|
| живая сеть | 0.51 с | **1.735 с** |
| **мёртвая сеть** | 0.52 с | **1.757 с** |

**Разница живая/мёртвая = 0.022 с (1.3 %)** — меньше внутреннего разброса. Было: до окна 11.8 с,
а при мёртвой сети до нуджа ~12 с сверху.

**Закрыто по дороге:**
- **TOCTOU** (команда по скелету, принятая после финализации) — отдельным `build_token`, у скелета
  его нет физически. Доказано сквозным сценарием на НАСТОЯЩИХ `EngineClient` + `validate_command`;
- **замерзание скелета** (замыкание по `source_rev` вернуло бы скелет навсегда) — фазозависимое
  замыкание + resume с сохранением токена; статус в условии несущий (иначе пропущенная книга воскресает);
- **`_files_are_stable` сравнивал ГОТОВЫЙ список** (существующий баг, не от D17) — теперь
  перечитывает каталог; каталог не читается → `None` (никогда не равно ничему);
- **ложная карточка «нет доступа к папке»** при мёртвой сети — доказано негативным контролем:
  вернули веб внутрь `_finish_manifest` → `exit 75`, `folder_access='blocked'`, дедлайн пробит;
- **второй подъём окна:** ключ фронта в приложении сделан **побайтовым зеркалом** ключа леджера
  агента (`NudgeEdge` ↔ `_book_edge_key`/`_group_edge_key`). Держится по построению, а не по совпадению;
- **`@State` съедал обновления** — слияние вынесено в чистую `ConfirmMerge.merge/seed` с матрицей
  pristine/dirty (харнесс 62 проверки);
- **дефект от самого D17:** бейдж над обложкой появлялся только на `ready` и толкал вниз правую
  колонку (раньше не видно — окно открывалось уже с обложкой). Слот зарезервирован невидимым двойником.

**Решения человека:** список глав из имён файлов сразу (пересорт при ID3 принят) · лента обложек
**append-only** (веб в конец, доказано префиксом) · **dirty-поля переживают смену `source_rev`**
(набранный текст не должен исчезать из-за фонового события; полный пересев только при смене `book_id`).

**Решения Юрки:** подсказка «Ищем обложку в интернете — можно не ждать» по полю `cover_web`
(`pending`/`done`), **но она НЕ ИМЕЕТ ПРАВА влиять на кнопку** — иначе работа M-B обнуляется;
повтор веб-поиска ≤3 раз (ноутбук просыпается раньше вайфая) — принято.

**Гейты сейчас:** `build-app.sh` exit 0 / 0 warnings · `selfcheck_app_routing` 320/320 ·
`selfcheck_all` **21/21** (после M-D станет **22/22**) · харнессы M-C 62/62 и 9/9.

**✅ M-D ЗАКРЫТ.** `agent/selfcheck_early.py` — 1019 строк, **93 проверки** (I1:15 · I2:6 · I3:8 ·
I4:13 · I5:7 · I6:9 · I7:2 · I8:9 · I9:13 · §0 критический путь:11). `app/selfcheck_wire.swift` —
провод `EngineClient`→диск. Харнесс M-C перенесён в репо значением (скретчпад был сессионный).
`app_routing` **320 → 423**. `nudge` 41→48 · `skip` 26→32 · `reconvert` 34→38 · `reliability` 16→18.

**§0 держит замеры СТРУКТУРНО, не секундомером:** порядок `скелет → нудж → ffprobe → chapters →
ready` + счётчик обращений к вебу (ровно 0 до `ready`). Секундомер на загруженной машине флакует.

**Неложность доказана: 16 мутаций, ноль выживших** — в т.ч. веб обратно в `_finish_manifest` (7
красных), снят гейт `build_token` (8), перечеканка `confirm_token` на ready (13), замыкание не
фазозависимо (12), веб-находки в начало ленты (2), ключ приложения обрезан 16→12 (1).
NB: **первая редакция I5 была ложной** (0 красных на мутации) — тестировщик поймал сам и переписал.

**ГЕЙТ: 22/22 сьют, три прогона** (235.9 / 237.5 / 237.7 с), exit 0, `blast_radius` чист.
`build-app.sh` exit 0, 0 warnings. Лестница показа окна цела.

### 📦 СБОРКА ГОТОВА К ЖИВОЙ ПРОВЕРКЕ (2026-07-28, ~07:5x)
- `build/dist/mp3-to-m4b.app` — 1.0, universal, границы helper'а 1–3 ✅
- `build/dist/mp3-to-m4b-1.0.dmg` — **4.8M, sha256
  `2e32747c7c1a14d7ab380af4129d18ec0ed69f6373f924ac7f73e930b73089a8`**, граница 4/4 ✅
  (заменил ночной `d73340fd…`, тот — только фикс окна, без D17)

### 🎉 РЕЛИЗ 1.0 ОПУБЛИКОВАН (2026-07-28)
- **Живая проверка человеком пройдена:** «теперь работает как надо».
- `origin/main` = `921a9cc` (5 коммитов ушли: `236e9a7..eccdd9e` + заметки).
- Тег **`v1.0`** + **GitHub Release**: https://github.com/ArrivaRUS/mp3-to-m4b/releases/tag/v1.0
  (не черновик, не предрелиз; вложены `mp3-to-m4b-1.0.dmg` 4.8 МБ + `.sha256`).
- `docs/release-notes-1.0.md` переписан под D16/D17, прогнан через `humanizer`, прочитан Юркой
  целиком перед публикацией (репозиторий публичный). Хвост «Коротко» ушёл в аннотацию тега,
  в тело релиза не попал.

### 🔢 Anti-drift (сессия 2026-07-26/28)
- `delegations` = **16 замерено после compaction** (≈61 за проект); до compaction — больше,
  `duration_ms` не сохранился.
- `yurka_direct_actions` = **0**. Своей рукой только: сборка/релиз-команды, запись памяти
  (HEARTBEAT, `decisions/log.md` D16–D17, `.patches/005–007`), суд двух архитекторов и
  синтез-план, разбор системного журнала (верификация), микроправка-пометка в `arch/`.

### ⏭ ХВОСТЫ НА СЛЕДУЮЩУЮ СЕССИЮ
1. **Тяжёлый случай лестницы окна живьём НЕ проверен:** в ребут-тесте №2 окно стало видимым
   базовым путём за 0.36 с (экран был пуст). `orderFrontRegardless()` и подскок дока
   подтверждены только юнит-тестами. Повторить с чужим полноэкранным окном поверх.
2. **Перенос к соседу `fb2-to-epub`:** та же болезнь — `app/main.swift:634, 954, 1161, 1541`
   четыре `activate(ignoringOtherApps:)`, `orderFrontRegardless` нет нигде. Там же висит
   незамерженная ветка `fda-probe-watchdog` (`9e3edc5`) — свести одним заходом.
3. **После 1.0:** мутационное тестирование как постоянный инструмент (себя показало: 16 из 16
   мутаций пойманы) · fsync родительского каталога в `write_json_atomic` · транзакция
   «манифест + состояние» · `scenario_latch` сверяет mtime боевого plist дословно ·
   `installer_repoint:379` пишет в реальный `$HOME` вне наблюдения `blast_radius`.
4. У человека в «Файлы и папки» остались две мёртвые записи `mp3-to-m4b-agent-t0` / `-t1`.
5. В папке Юрки: ветка `session/yt-link-clickability-finding`, коммит `52b9075` (починка
   рецепта код-ревью в `codex-bridge`) — **закоммичен, НЕ запушен**. Рядом незакоммиченные
   правки человека (`tools/yt.py`, `HQ/now.md`, `telegram-bridge`, `launch.json`) — не трогать.

---

### 🔴 (ИСТОРИЯ) РЕБУТ-ТЕСТ №1, 2026-07-27 ~21:30 — ПРОВАЛЕН, причина найдена и починена

**СТАТУС (2026-07-27, ~23:4x): ФИКС ГОТОВ, СОБРАН, ЖДЁТ ЖИВОГО РЕБУТ-ТЕСТА ЧЕЛОВЕКА.**

Сделано и проверено:
- ревью правок — **GO**; инвариант «прогон в полёте» = `presentationCheck != nil` доказан полным
  перебором достижимых состояний (их ровно 4), гонок нет, ретейн-циклов нет;
- сторожи проводов: `app/selfcheck_routing.swift` → **320/320**, доказаны неложными (15 проводов
  сломано по одному — все покраснели; 2 контроля — зелёные). Мутант нашёл дефект в самом стороже
  (искал `window.makeKeyAndOrderFront(` с получателем, `window?.` проходил насквозь) — переписан;
- **флак в гейте устранён:** `selfcheck_installer_repoint` держал ПРИВАТНУЮ копию сторожа боевого
  дерева с наивным mtime → ~7 % ложных красных (боевой агент тикает раз в 300 с и трогает свой лог).
  Копия удалена, сьюта зовёт общий `blast_radius.snapshot()/diff()`. Доказано двусторонне:
  46 прогонов зелёные (4 проверяемо попали на настоящий тик, старый код на них краснеет — симптом
  91/92 воспроизведён), 10 внесённых утечек по-прежнему ловятся. Урок: `.patches/005`, рецидив + правила 8–9;
- **гейт на финальном дереве: 21/21 сьют, 780 проверок**, `blast_radius`: «no self-check touched
  the user's install».

**СБОРКА ГОТОВА (2026-07-27 ~23:4x):**
- `build/dist/mp3-to-m4b.app` — 1.0, universal, ad-hoc подпись, границы helper'а 1–3 ✅
- `build/dist/mp3-to-m4b-1.0.dmg` — **4.7M, sha256 `d73340fd00df3959f09b39df972e288930613ac91d45189f58d4efb4f3db8ba1`**,
  граница 4/4 (helper внутри смонтированного образа) ✅. ЗАМЕНИЛ старый образ от 26.07 (`3114023e…`).
- Проверено, что фиксы реально внутри бандла: `WindowPresentation` в бинаре, исправленный
  `selfcheck_installer_repoint.py` в `Contents/Resources/agent/`.

⚠️ **ПОРЯДОК РЕБУТ-ТЕСТА (важно, иначе тест ничего не докажет):**
1. Человек ставит НОВЫЙ образ в /Applications (поверх старого) и запускает один раз — приложение
   переустановит агента (это штатно).
2. Перезагрузка.
3. **Класть НОВУЮ папку с mp3, а НЕ уже ждущую книгу.** Леджер агента даёт «одна публикация = не
   более одного запуска» — уже опубликованная книга повторно НЕ нуджит, и тест будет ложно-красным.
4. Критерий: окно ВИДНО без единого клика, замер «от дропа до видимого окна» (в прошлый раз 85 с,
   ожидаем ≤2.5 с). Фокус приложение забрать НЕ обязано — на Tahoe это недостижимо (`.patches/006`).

**В watch-папке лежит книга человека «Вергезе Абрахам — Рассечение Стоуна» в `pending-confirm`**
— НЕ удалять. Её можно собрать до теста (тогда папка чистая), либо оставить и класть вторую.

⚠️ **К КОММИТУ:** `app/WindowPresentation.swift` и `.patches/006-*.md` — **untracked**, нужен
`git add` (правило §5.1 «память проекта в git»).

⚠️ **ЛОВУШКА ДЛЯ СЛЕДУЮЩЕЙ ДИАГНОСТИКИ:** лестница зовёт `NSApp.activate()` на каждом входе →
в unified log будет СЕРИЯ `CPS: Rejecting … activation count being 0`. Это НОРМА, а не поломка.
Признак успеха — переход окна `occluded → visible` / `orderFrontRegardless` в трассе. Подробнее
в `.patches/006`. И `log show` звать ТОЛЬКО как `/usr/bin/log` — в zsh встроенный `log` тенит
бинарь и даёт молча неполную выборку (на этом я и построил первый, неверный диагноз).

**Готово по фиксу:**
- `app/WindowPresentation.swift` (новый, 1–142) — чистый решатель лестницы, Foundation-only;
- `app/main.swift` +214/−12: `presentWindow()` = единственная точка показа (зовут 5 путей),
  `bringWindowForward()` УДАЛЁН, добавлен `applicationShouldHandleReopen` (раньше его не было),
  наблюдатель `NSWindow.didChangeOcclusionStateNotification`, `NSApp.activate()` под
  `#available(macOS 14.0)` (цель сборки 11.0, старая ветка сохранена);
- тесты: `app/selfcheck_routing.swift:232–572` +62 проверки → `selfcheck_app_routing` **295/295**,
  общий гейт `selfcheck_all` **21/21** (в реестре 21 сьюта, старая цифра «20/20» ниже — устарела),
  blast_radius чист. **Мутационная проверка тестов:** 6 внесённых дефектов правила пойманы,
  2 подстройки констант — нет (тест привязан к смыслу, не к литералам). Харнесс в скретчпаде,
  в репо не заводили — результат зафиксирован текстом в `test-plan.md:41–69`;
- код-ревью: **GO с правками**, блокеров нет; Codex-проход (gpt-5.5) сверх ревьюера не нашёл ничего.

**Решения Юрки по ревью (не пересматривать):** короткое замыкание `isActive || isVisible → satisfied`
ОСТАЁТСЯ (в отказном сценарии активация отклонена ⇒ `isActive == false`, замыкание не срабатывает;
матрицу из 16 случаев не ломаем) · `bringWindowForward()` не возвращаем · мутационный харнесс в
проект НЕ заводим сейчас (изменение процесса на релизном пути — отложено, см. `decisions/log.md`).

**✅ ЗАКРЫТО (2026-07-27):** `presentWindow()` зовёт `unhideWithoutActivation()` — новая книга
РАЗ-ПРЯЧЕТ приложение, спрятанное через Cmd+H. Спросил человека — **он этой командой не
пользуется и не знает её**, вопрос снят, оставляем как есть. NB на будущее: не выносить человеку
вопросы про горячие клавиши/системные жесты — он ими не оперирует, спрашивать надо про ПОВЕДЕНИЕ.

**✅ ДИАГНОЗ ПОДТВЕРЖДЁН (дебагер `aeaf45de3676dd0d2` + Codex GPT-5.6 Sol, unified log):
окно СОЗДАЛОСЬ за 0.6 с, но осталось ПОЗАДИ фокусного приложения. Процесс не умирал.**
Причина — **кооперативная активация macOS 26**: у программно запущенного процесса нулевой
кредит активации. `CPS: Rejecting the request for pid 2770 due to the activation count being 0;
window count = 1`. Флаг `activate(ignoringOtherApps: true)` на Tahoe — **no-op**. «Смерть через
30 с» = включение App Nap (система сочла приложение невидимым). Человек кликнул сам в 21:15:11 →
`occluded → visible`, вышел в 21:15:28 закрыв окно. Мои гипотезы (синхронная установка,
терминация, роутер, грейс, дубли бандлов) — ВСЕ опровергнуты. Урок: `.patches/006`.

#### Ребут-тест 2026-07-27: агент ✅, приложение ❌
Человек перезагрузился (20:56:42) и в 21:13 положил книгу в `~/Desktop/mp3-to-m4b`.
**АГЕНТ ОТРАБОТАЛ ИДЕАЛЬНО — исходная жалоба «агент не просыпается» ЗАКРЫТА:**
- 20:57:13 `agent_started` (RunAtLoad после ребута) · тики 21:02/21:07/21:12 (StartInterval=300)
- 21:13:32 сработал WatchPaths на дроп · `books found: 1` · `folder access: ok`
- 21:13:45 `app_nudged` rc=0 · книга `pending-confirm` · поколение совпало
- launchd: job загружен, PA0 = helper, runs=5, last exit 0

**СЛОМАЛОСЬ ПРИЛОЖЕНИЕ (окно было НЕВИДИМО, а не отсутствовало — см. диагноз выше):**
- `21:13:45.891` LaunchServices подняла ПРАВИЛЬНЫЙ бандл `/Applications/mp3-to-m4b.app`, pid 2770
- `21:13:46.343` AppKit: `_NSDisableAutomaticTerminationAndLog "No windows open yet"`
- `21:13:46.408` WindowServer: `presents 0 windows … Denying the request` (LS-активация обогнала окно)
- `21:13:46.467/.483` `window count = 1` + `activation count being 0` → **наш `activate` отклонён**
- `21:14:16.717` App Nap (НЕ смерть процесса), краш-репортов НЕТ — их и не было откуда взяться
- когда Юрка запустил руками в 21:15 — окно показалось И приложение переустановило агента
  (receipt переписан, `installed_at` 21:15:23, поколение `CD41EB40…`→`54A910F1…`, mode full)
- версии: staged 1.0, бандл 1.0, но ОТПЕЧАТКИ разошлись (пересборка 18:0x с правками M4/M6
  поверх установки 13:59) ⇒ приложение пошло по пути авто-обновления staged-агента

**ПЛАН ФИКСА (утверждён Юркой, отдан разработчику; правки только в `app/`):**
1. `app/WindowPresentation.swift` — ЧИСТЫЙ решатель лестницы эскалации (под автотест).
2. `presentWindow()` — единая точка показа: unhide → deminiaturize → клэмп на экран
   (`WindowGeometry`) → `makeKeyAndOrderFront` → `NSApp.activate()` (новый API, без аргумента).
3. **Самопроверка видимости** (лекарство от «тихого зелёного»): отменяемый таймер ~1 с
   (сброс по `didBecomeActive`/`didChangeOcclusionState`); если `!isActive && !occlusionState.visible`
   → `window.orderFrontRegardless()` (поднимает поверх чужих окон БЕЗ активации); ещё ~1.5 с →
   `requestUserAttention(.informationalRequest)` РАЗОВО.
4. Та же лестница в тёплом пути `bringWindowForward()` (main.swift:5526) + новый
   `applicationShouldHandleReopen`.
5. Агента и D13 НЕ трогаем: видимость знает только приложение (`occlusionState`), эскалация — в нём.
   `app_nudged` = «LS приняла запрос», это честно и большего не утверждает.

⚠️ **Гарантированный фокус без действия человека на Tahoe НЕДОСТИЖИМ** — политика ОС.
Цель формулируем как «окно ВИДИМО и не перекрыто», а не «окно перехватило клавиатуру».

**🔁 ПЕРЕНОС К СОСЕДУ (после того как фикс докажет себя здесь):** `fb2-to-epub` болен тем же —
`app/main.swift:634, 954, 1161, 1541` = четыре `activate(ignoringOtherApps: true)`,
`orderFrontRegardless` нет нигде, `applicationShouldHandleReopen` (1538) зовёт тот же no-op.
Там же висит незамерженная ветка `fda-probe-watchdog` (`9e3edc5`) — свести в один заход.

**Сделано попутно (мной, вне бага):** удалён дубль-бандл `build/dist/mp3-to-m4b 2.app`,
создан `build/dist/.metadata_never_index`, сняты регистрации из LaunchServices — было ТРИ
бандла с одним bundle-id (система выбирает нестабильно). В этот раз не причина, но мина.

**Состояние боевой установки:** рабочая, грант выдан, `folder_access: ok`. В watch-папке лежит
**книга человека** «Вергезе Абрахам — Рассечение Стоуна» в статусе `pending-confirm` — НЕ
удалять, он может её собрать. Окно подтверждения поднято.

**Git:** 4 коммита не запушены (`ffbeb36` M0 · `60b0450` M1–M6 · `2676973` версия 1.0 ·
`b351f01` FDA-инструкция + blast_radius). origin/main = `236e9a7`. Публикация ЗАДЕРЖАНА до фикса.
DMG `build/dist/mp3-to-m4b-1.0.dmg` (sha `3114023e…`) собран, но **устарел** — после фикса пересобрать.

---

### (архив) ТЕКУЩИЙ МОМЕНТ 2026-07-26 ~17:30 — два UI-бага, ЗАКРЫТЫ коммитом b351f01

**Что делается прямо сейчас:** фоновый агент-разработчик (M6) чинит ДВА бага, которые человек
нашёл руками в боевой 1.0 — в раскрывашке «Включить доступ вручную в Системных настройках»:
1. **Ссылка вела в НЕ ТОТ раздел.** Было `Privacy_AllFiles` («Полный доступ к диску»), а наша
   запись — `kTCCServiceSystemPolicyDesktopFolder`, то есть раздел **«Файлы и папки»**. Записи
   `AllFiles` для helper'а не существует вообще. ✅ **Anchor уже починен** (`Privacy_FilesAndFolders`
   в `app/main.swift`), осталось переписать шаги внутри раскрывашки под новый раздел.
2. **Кнопка «Скопировать путь агента» молчит.** Копирует ИСПРАВНО (проверено: путь в буфере,
   PA0 совпал) — но нет никакой обратной связи, поэтому читается как сломанная. ⏳ **ack ещё НЕ
   сделан.** Нужен видимый ответ и на успех, и на fail-closed отказ (сейчас он только в NSLog).
Агент предупредит ПЕРЕД живой проверкой якоря (открытие Системных настроек видно на экране —
правило после T0: предупреждать человека).

**Незакоммичено:** `HEARTBEAT.md` + правки агента (в работе).
**Не запушено (3 коммита):** `ffbeb36` (M0) · `60b0450` (M1–M6) · `2676973` (версия 1.0).
origin/main = `236e9a7`. Пуш и публикация — ТОЛЬКО по явному «да» человека.

**ПОРЯДОК ДО КОНЦА:**
1. Дождаться агента → пересобрать (`build/build-app.sh` && `build/make-dmg.sh`, 4 стража) →
   переустановить в /Applications → **показать человеку исправленную инструкцию на вычитку**.
2. **Ребут-тест (последняя проверка, за человеком):** перезагрузка → **НЕ открывая приложение**
   кинуть папку с mp3 в `~/Desktop/mp3-to-m4b` → окно должно всплыть само (потолок 5 мин,
   StartInterval=300). Это закрывает исходную жалобу «агент не просыпается после перезагрузки».
   ⚠️ После ребута сессия оборвётся — человек скажет «продолжаем релиз», всё есть здесь.
3. Коммит фиксов → пуш 4 коммитов → тег `v1.0` → GitHub Release с
   `build/dist/mp3-to-m4b-1.0.dmg` + текстом из `docs/release-notes-1.0.md` — по «да».
4. Финал: HEARTBEAT, `time.local.md`, показать человеку тайм-сводку (правило CLAUDE.md).

**Про мою (Юркину) роль в этих двух багах — записать в урок:** оба нашёл ЧЕЛОВЕК за минуту
живой работы, хотя я гонял живую сверку карточки. Я проверял состояния автомата, но НЕ открыл
раскрывашку до конца и НЕ нажал кнопку внутри неё. Правило: живая сверка обязана доходить до
ЛИСТЬЕВ — вложенные блоки, кнопки внутри раскрывашек, ссылки наружу. Текст инструкции устарел
ровно потому, что писался под FDA-модель, которую мы В ЭТОЙ ЖЕ сессии отменили находкой №2 T0.

### ОСТАЛОСЬ ДО РЕЛИЗА (по порядку)
1. **Версия 0.9 → 1.0** (микроправки Юрки, НАЧАТО — grep уже сделан, файлы найдены):
   `app/Tokens.swift:60` appVersion · `agent/__init__.py:28` __version__ ·
   `build/build-app.sh:47` дефолт · `build/make-dmg.sh:38` дефолт.
2. Пересборка: `build/build-app.sh` → `build/make-dmg.sh` (4 стража байтов обязаны быть зелёными).
3. Полный гейт `python3 -m agent.selfcheck_all` (20/20, ~5-7 мин).
4. Коммит версии (микро) — можно вместе с релизным.
5. **БОЕВАЯ УСТАНОВКА у человека** (впервые с 13.07!): смонтировать DMG → .app в /Applications
   → человек запускает (правый клик → Открыть, ad-hoc) → Setup → «Завершить установку» →
   **появится системный диалог** «Приложение «mp3-to-m4b-agent» запрашивает доступ…» →
   человек жмёт «Разрешить» (возможно ДВА диалога подряд — Рабочий стол и/или Загрузки).
   ⚠️ Предупредить человека ЗАРАНЕЕ про диалог (правило после T0).
6. **Негативный контроль** (человек, ~2 мин): Системные настройки → Конфиденциальность →
   Файлы и папки → выключить тумблер у mp3-to-m4b-agent → приложение обязано показать карточку
   «Доступ запрещён» (НЕ молчать) → включить обратно → «Проверить снова» → ok.
7. **Ребут-тест** (закрывает исходную жалобу): перезагрузка → НЕ запуская приложение, кинуть
   папку с mp3 в `~/Desktop/mp3-to-m4b` → книга должна собраться сама (окно всплывёт).
   StartInterval=300 — потолок задержки 5 мин, если WatchPaths проспал.
8. Пуш `ffbeb36`+`60b0450`+релизный коммит, тег v1.0, GitHub Release с DMG — по явному «да».
9. Хвосты после релиза: HEARTBEAT-финал, time.local.md, показать тайм-сводку.

### ХВОСТЫ (не забыть)
- У человека в Системных настройках → Файлы и папки висят 2 мёртвые строки от T0-стендов:
  `mp3-to-m4b-agent-t0` и `-t1` — попросить убрать при удобном случае (файлы удалены).
- Сосед fb2: ветка `fda-probe-watchdog` (коммит `9e3edc5`, фикс мины-висяка) — НЕ смержена,
  НЕ запушена. После нашего релиза — предложить человеку PR тем же порядком, что у него принят.
  Плюс хвост соседа: текст карточки для `blocked` (копирайт «macOS ждёт разрешения»).
- 133 свежих temp-каталога в TMPDIR от последних прогонов — добрать при уборке.
- В `.venv`-каталог соседа не лазили; наш `build/.venv` в .gitignore.

### КЛЮЧЕВЫЕ ФАКТЫ ДЛЯ НОВОГО КОНТЕКСТА (чтобы не переоткрывать)
- Helper: `packaging/mp3-to-m4b-agent`, sha256 `791d020d42477755fe3c46070699421280c2dd7e5f248da59f3f826a5bdbc079`,
  в бандле `Contents/Resources/mp3-to-m4b-agent`. Байты ЗАМОРОЖЕНЫ (грант = байты+путь).
- FDA НЕ нужен: хватает папочного согласия (диалог). Скриншот: `docs/consent-dialog.png`.
- 4 состояния folder_access: ok/denied/blocked/missing (agent/scan.py:274-281; Swift ассертит).
- Изоляция тестов — в каркасе selfcheck_all (перенаправления + blast_radius). Ad-hoc python3 -c
  по коду агента — ЗАПРЕЩЁН без полного набора env (урок .patches/005).
- Правило: на релизном пути предупреждений не бывает — либо гейт, либо не проверяем
  (4 «тихих зелёных» найдено и закрыто за сессию).
- Watch-папка остаётся `~/Desktop/mp3-to-m4b` (T0.7 ok/ok/ok, iCloud-гипотеза опровергнута).

**T0.7 (2026-07-26, 10:28): `GATE → CLOUD → GATE = ok → ok → ok`.** Находка 3 (iCloud) —
артефакт порядка ячеек, НЕ подтвердилась. **Дефолт `~/Desktop/mp3-to-m4b` остаётся**, план Б
(§2 аддендума) не нужен. Идентичность прогона `mp3-to-m4b-agent-t1`, sha `791d020d…`,
subject/responsible = helper во всех ячейках, python читал маркер за 9 мс.
NB: перезагрузки не было (машина up с 24.07) — не понадобилась: свежая личность `-t1` без
записей в TCC снимает «отравленный путь» по построению; контрольный повтор GATE подтвердил
отсутствие дрейфа.

**🔑 ГЛАВНОЕ ДЛЯ M6 — скриншот диалога согласия: `docs/consent-dialog.png`.**
macOS показывает **имя файла**, а не путь и не signing identifier:
«Приложение **«mp3-to-m4b-agent-t1»** запрашивает доступ к файлам в Вашей папке «Загрузки».»
[Не разрешать] [Разрешить]. Значит **поход в Системные настройки не нужен** — онбординг
сводится к «кинул папку → система спросила → Разрешить». FDA остаётся фолбэком.
⚠️ Имя helper'а в бою = `mp3-to-m4b-agent` — оно ЗАМОРАЖИВАЕТСЯ вместе с путём при первом
боевом гранте (решение Юрки: оставляем, согласуется со словарём приложения «Фоновый агент»).

**Хвост для человека:** в **Системные настройки → Конфиденциальность и безопасность →
Файлы и папки** (НЕ «Полный доступ к диску» — записи папочные) убрать 2 строки:
`mp3-to-m4b-agent-t0` и `mp3-to-m4b-agent-t1`. Файлы уже удалены, записи висят в пустоту.

### Почему это делается
На macOS 26 грант FDA shell-скрипту **мёртв как класс** (tccd атрибутирует к `/bin/bash`).
У нас `ProgramArguments[0] = bin/runner.sh` + watch-папка в `~/Desktop` (TCC-зона) ⇒ «положил
папку — ничего не происходит». Сосед fb2-to-epub прошёл это боем (его `.patches/020`).

### Сделано в этой сессии
- **Аудит соседа** (2 агента): нашли 3 живых бага 0.9 + этот класс-блокер + 20 уроков соседа
  против наших 4.
- **Коммит `c043d5b`** — 5 исправлений + команда `skip`: мёртвые кнопки (apply-all работает,
  «Пропустить» реализована сквозняком: статус `skipped`, секция ПРОПУЩЕНО, «Вернуть»,
  воскрешение повторным дропом обеими формами) · подмена книги в очереди (`onConfirm: { _ in }`
  выбрасывал аргумент, «N из M» был литералом) · sudden-termination (Cmd-Q убивал уборку) ·
  клэмпы окна по обеим осям → `app/WindowGeometry.swift` + юниты. Гейт **18/18 сюит**.
- **Коммит `236e9a7`** — планы: `arch/plan-binrunner-mp3-{claude,codex,v2}.md` (двойной
  архитектор + адверсариальный разбор Codex: 5 BLOCKER · 12 MAJOR · 2 MINOR + синтез 615 строк).
- **M0 ЗАКРЫТ (не закоммичен!):** замороженный Mach-O helper `packaging/mp3-to-m4b-agent`,
  sha256 **`791d020d42477755fe3c46070699421280c2dd7e5f248da59f3f826a5bdbc079`**, оба известных
  дефекта донора закрыты ДО заморозки (микрофиксы №1 pid-reuse и №2 пре-spawn сигналы;
  мутационная проверка поймала ложнозелёный тест). `bin/runner.sh` переведён на форму донора
  (python в фоне + `wait` + trap-цикл, НЕ `exec`). Харнес `packaging/agent-src/t0/`.
- **T0 ЗЕЛЁНЫЙ:** grant вешается на helper (5 мс), тот же грант через `runner.sh` → deny,
  subject `/bin/bash`; чужой бинарь по тому же пути → deny (привязка к байтам). Сверено
  журналом tccd (один msgID) + декодом `csreq` в cdhash.
- **Иконка:** `build/.venv` пересобран (Homebrew-питон исчез с машины, остался системный 3.9.6);
  `build-app.sh` больше НЕ подменяет cairosvg на sips молча — падает с диагнозом, `--allow-sips`
  для dev.
- **Аддендум** `arch/plan-binrunner-mp3-v2-addendum.md` (481 стр.) — три находки T0.

### Три находки T0 (меняют план)
1. **Helper без гранта ВИСНЕТ, а не получает deny.** Он атрибутируемый (не платформенный) ⇒
   политика «спросить человека» ⇒ `open()` не возвращается. Фоновый агент диалог показать не
   может. Нужен сторож пробы + ТРЕТЬЕ состояние `blocked` (≠ `denied`).
2. **FDA не нужен** — хватает папочного согласия (`auth_reason=2`, диалог). Проверено по TCC.db:
   записи `…DesktopFolder`/`…DownloadsFolder`, `…AllFiles` нет вообще. Упрощает онбординг M6.
3. **iCloud-Desktop — НЕ ПОДТВЕРЖДЕНО** (архитектор опроверг): cloud-ячейка мерила отравленный
   путь после разрушающей D; в том же состоянии и локальная папка давала `blocked`. Контрпример:
   сосед fb2 прямо сейчас читает И пишет `~/Desktop/fb2-to-epub` в том же домене. **Решение
   человека: дефолт `~/Desktop/mp3-to-m4b` ОСТАВЛЯЕМ.** T0.7 — чистая перепроверка.

### Ловушка миграции (если дефолт когда-нибудь сменится)
`book_id = sha256(абсолютный путь)` (`scan.py::book_id_for`), а `.m4b` пишется ВНУТРЬ
watch-папки ⇒ переезд меняет все `book_id` и перевзводит библиотеку. Переносить только
несобранные исходники, готовые оставлять, ledger'ы пересобирать.

### Хвосты
- Человеку осталось убрать строку `mp3-to-m4b-agent-t0` в **Системные настройки →
  Конфиденциальность и безопасность → Файлы и папки** (НЕ в «Полном доступе к диску» —
  записи папочные). Файл уже удалён, записи висят в пустоту.
- **Незакоммичено:** весь M0 (helper, runner.sh, харнес, PROVENANCE, build-app.sh, аддендум).
- **Мина у соседа ЗАКРЫТА (но НЕ закоммичена в его репо):** `fb2-to-epub` —
  `probe_watch_dir_access()` делал `os.listdir` без сторожа ⇒ в день пропажи записи в TCC
  завис бы молча. Добавлен сторож (дедлайн 2 с, `os._exit(0)` в подпроцессе пробы) + третье
  состояние `blocked` (≠ `denied`) + ранний `exit 75` до первого glob'а + Swift-предикат
  `blocksConversion` во всех 4 потребителях. Тесты 53/53 (было 30); негативный контроль против
  до-фиксного watcher'а: 32 passed / **17 failed** — мина была реальной. Изменены:
  `bin/fb2-to-epub-watcher.sh`, `app/{StateModel,StatusView,main}.swift`, `tests/*`.
  **Хвосты у соседа:** текст карточки для `blocked` (нужен другой копирайт: «macOS ждёт
  разрешения», а не «идите в Системные настройки») · остаточная гонка «проба сказала ok, грант
  исчез через секунду» — закрывается только сторожем на всю фазу прогона · урок-патч про
  апостроф в heredoc внутри `$( )`, который ломает разбор bash (ловится только `bash -n`).
- Дальше по плану v2: M1 installer → M2 build/DMG → M3 runner/сигналы → M4 агент (сторож+Р3) →
  M5 Swift-истина → M6 UI доступа → M7 релиз (R1b — грант на боевых байтах).

---

## Что это
macOS-утилита: слежение за выбранной папкой → сборка mp3 в аудиокнигу **.m4b**
(главы + обложка), с **обязательным подтверждением** качества и обложки во
всплывающем окне ПЕРЕД конвертацией. Функциональный клон каркаса соседа
**fb2-to-epub** (нативное SwiftUI-приложение + фоновый launchd-агент + `state.json`),
движок fb2→epub заменён на mp3→m4b.

## Фаза
ФАЗА 4 (разработка). Дизайн ✅ (G3) · **архитектура ✅ (G4 принят)** — синтез `arch/synthesis.md`.
**M0 ✅ · M0.5 ✅ · M1-вертикаль ✅** — реальная разведка (ffprobe) и реальная сборка `.m4b` доказаны (selfcheck M0 36/36 · M0.5 23/23 · M1 34/34; Юрка вскрыл боевую книгу: 5 глав 1/1000, обложка mjpeg attached_pic, brand M4A, +faststart, 44100/стерео, без дрейфа). **Окно §3 ✅** (4 состояния сверены вживую по window-id; кнопки converting/error отложены — D13). **Цепочка обложки ✅** (движок Pillow+веб + пикер в окне + выбор едет в `confirm-build` params → `build_m4b` вшивает ИМЕННО выбранную; `selfcheck_cover_pick` 16/16 pixel-доказал; Юрка сверил пикер вживую). **Группировка ✅** (loose-mp3 в корне → sheet D1 → `grouping-choice` → агент материализует combine→1 / separate→N книг; `selfcheck_grouping` 22/22; Юрка поймал баг наложения чипов вживую → 2 фикса, урок `.patches/002`: вёрстку сверять глазами, SwiftUI-флоу не самосайзится по высоте). **Очередь (display) ✅** (4 секции по статусам + batch-чип + навигация «Позже в очередь»; `selfcheck_queue` 18/18; Юрка self-check'ом вскрыл баг — диспетчер не обновлял витрину при `converting` → фикс `scan.refresh_showcase()` на всех переходах; регрессии зелёные). **Status (spec §5) ✅** (hero+кольцо c `totals.built`, 3 стат-карты, агент проецирует `totals.built/today`+`engine`; `selfcheck_status` 21/21; «глава X из Y» нет → «Сейчас:<книга>»+общий прогресс; наезд «последних книг» на футер ПОЧИНЕН — список height-capped+скролл над футером, Юрка сверил на 6 книгах; долг: тот же паттерн в `QueueView` — чинить в слое cancel). **⚡ Self-check'и развязаны ✅** — вложенные регрессии (~30 мин) убраны, плоский `selfcheck_all` за **~2 мин** (Юрка подтвердил). **Отмена сборки (D13) ✅** — кооперативная (Popen+опрос, агент гасит свой ffmpeg, без kill по pid; converting→pending-confirm; danger-кнопка в QueueView + наезд QueueView починен; `selfcheck_cancel` 16/16 — нет осиротыша/частичного файла; `selfcheck_all` **9/9**; Юрка сверил вживую на 9 книгах + cancel-команда корректной формы). **Надёжность (E1–E18) ✅** — аудит (12 уже-обработанных) + фиксы 3 пробелов: E3 битый→вся книга в error (решение человека), E5 нет места→`no_space` danger+cleanup до ffmpeg, E10 недокопированный→дебаунс в скане; E15 порог<главы→DEFER до нарезки. `selfcheck_reliability` 16/16, полный гейт `selfcheck_all` **10/10** (216s, Юрка подтвердил). **Нарезка (P1) ✅** — движок `split.py` (plan_parts+split stream-copy §3, `-map_chapters 1`, oversize E15) + UI-панель (слайдер 100–700МБ + превью «≈N частей»+чипы, `SplitPlanner` порт plan_parts); `selfcheck_split` 26/26, Юрка вскрыл части ffprobe (без дублей глав, stream-copy, обложка/title) + сверил панель вживую; `selfcheck_all` **11/11**. **Упаковка ✅** — `packaging/installer.sh` (ffmpeg-детект, venv+Pillow, launchd WatchPaths=папка+commands, plutil) + `build/build-dmg.sh`; **Setup-экран §01 ✅** (7-й из 7: ffmpeg-статус+выбор папки+«Установить»→запуск забандленного installer.sh; детект первого запуска; Юрка сверил вживую). **DMG `build/dist/mp3-to-m4b-0.1.0.dmg`** (3.4МБ, ad-hoc) ставится через окно без терминала; Юрка проверил безопасно (verify/монтаж/dry-run, систему не трогал). **🎉 M1 ПОЛНОСТЬЮ ЗАВЕРШЁН — все 7 экранов + функции + GUI-установка; `selfcheck_all` 11/11.** Осталось до релиза: **QA человеком на реальных книгах** (поставить DMG → Apple Books, test-plan §QA) · код-ревью (CodeReviewer∥Codex) → ⛔ **G5** (PR/релиз, СТОП — нужно «да»).

## 🚀 Релиз v0.9 + двуязычный README (2026-07-07)
- **v0.9 выпущен**: код+приложение `11f3610`, тег `v0.9`, GitHub Release + DMG (sha `7ea5ad90`). Версия сквозная — был баг «0.1.0» хардкодом в 3 Swift-файлах мимо `Tokens.appVersion` → фикс на интерполяцию версии; урок `.patches/003`.
- **Авто-всплытие на дроп**: агент будит приложение (`open -b <bundle>`) на rising-edge. Триггер = САМ факт дропа, НЕ «новизна» содержимого (правка человека). Два сигнала: inode+st_dev (source_rev v2 — Finder-копия даёт новые inode) + presence-ledger present→absent→present (ловит Finder-MOVE). Двойной архитектор: Codex #2 поймал дыру inode-only на MOVE → presence-ledger. Урок `.patches/004`; `selfcheck_nudge`; гейт `selfcheck_all` 16/16.
- **Полировка UI**: строка очереди «Готово» (иконка вместо «Открыть», brandCyan «Собрать заново»), длительность «56 глав · 25 ч 56 м», Cmd+Q (`installMainMenu` для raw NSApplication; урок `native-macos-app-needs-main-menu`), бейдж источника обложки справа от «ОБЛОЖКА», убран дубль «Из файла».
- **README (RU `README.md` + EN `README.en.md`) в стиле соседа fb2** + **4 скриншота интерфейса** (confirm/status/queue/settings, 800px; флуд-кроп окна из CleanShot-снимков — near-black заливка + отсечка по гистограмме) + **соц-баннер** `branding/github-social.png` 1280×640. Коммит `7dade90`, запушено; баннер залит человеком в GitHub Social preview.
- **Центровка баннера** (правка человека): текстовый блок был на ~39px ниже центра → brand-designer поднял (−39px по элементам + якоря пришпиленных градиентов grad-m4b/grad-arrow; waveform −16px), Юрка сверил рендер глазами. Коммит `85eccf6` — ⚠️ **локально, НЕ запушен** (ждёт «да» человека; следующая сессия — запушить, чтобы git совпал с залитым соц-превью).

## Сделано (2026-06-28)
- Интейк: папка-сосед создана, git init на `main`, репо `ArrivaRUS/mp3-to-m4b` (публичный).
- Скоуп-гейт пройден — 3 решения человека (D1–D3) + ядро ТЗ (D4–D6). См. `decisions/log.md`.
- Тех-разведка ✅ — `research/m4b-toolchain.md`. **Вывод: одного `ffmpeg`(+`ffprobe`) достаточно** (склейка→AAC concat-filter, главы FFMETADATA `1/1000`, обложка `attached_pic`, контейнер `-f ipod +faststart`, нарезка stream-copy + обязательный `-map_chapters`). Доп. зависимости не нужны; запасные обложки — cairosvg/pillow (квадрат 1:1). Грабли: raw-mp3 в m4b нельзя (только AAC); `libfdk` недоступен → встроенный `aac`.
- PRD / истории / беклог ✅ (`prd/`). **G1 принят (2026-06-28):** цепочка обложки и Status — в MVP (D7, D8); нарезка — v1.1. MVP = 26 Must.
- Логотип ✅ **G2a (2026-06-28):** направление **02 «книга + play»**, единое семейство с fb2, цвет cyan→teal→indigo (D9). Финал + лого-пак готовы (`branding/logo.svg`, `icon-app/small/mono/menubar`, `brand-basics.md`).
- Макеты ✅ **G2b принят (2026-06-28):** 7 экранов + флоу (`design/`). Правки приёмки: компактный блок качества + главы на всю высоту окна подтверждения (D10).
- Токены ✅ (`design/tokens.*`, ~210) + **дизайн-спец ✅ G3 (2026-06-28, D11)** — `design/spec.md` + референсы `design/refs/`. Правила реализации (cap высоты окна, `.contentShape`, имена под `Tokens.swift`) зафиксированы.
- Двойной архитектор ✅ (`arch/plan-claude.md` ∥ `arch/plan-codex.md`) + **синтез ✅ G4 (2026-06-28, D12)** — `arch/synthesis.md`. Каркас-клон fb2; агент-единственный-писатель + command-log/манифесты; Python-движок; обложки Pillow.

## Следующий шаг
1. **M0 ✅ закоммичен** (протокол-скелет, 6/6, доказан вживую). app/agent/bin/build в репо.
2. **M0.5** — реальная разведка: `probe.py`/`metadata.py` через **ffprobe** (длительности глав, ID3 автор/название, детект встроенной обложки) → манифест с реальными данными + длительностями в окне.
3. **M1** — `build_m4b.py` (ffmpeg → реальный `.m4b` с главами+обложкой) → окно подтверждения целиком → цепочка обложки (mp3→веб→Pillow) → группировка → очередь → Status → надёжность → нарезка (P1) → упаковка/QA → ⛔ **G5** (PR/релиз).
4. Ревью (CodeReviewer ∥ Codex) → тесты (Tester) → QA на каждом крупном куске.

## Принятые тех-дефолты (ратифицировать на G4)
- Кодек: встроенный `aac` (QA опц. сравнит `aac_at`). `stik=Audiobook` — НЕ в v1 (QA проверит распознавание в Apple Books; иначе добавить опц. AtomicParsley).
- Параметры по умолчанию: 192 kbps стерео, 44100 Гц. Нарезка — по умолчанию выкл.
- Обложки: из mp3 → веб (DuckDuckGo/Yandex «автор+название+аудиокнига» + litres + OpenLibrary) → генерация квадратных вариантов.

## Карта (откуда что брать)
- **Прообраз архитектуры:** `../2026.06 fb2-to-epub/` — `app/*.swift`, `bin/*`,
  `launchd/*`, `packaging/installer.sh`, `build/*`, `arch/*`, `.patches/*`.
- **Тех-разведка движка:** `research/m4b-toolchain.md`.
- **Решения:** `decisions/log.md`. **Уроки:** `.patches/NNN-*.md`.

## Счётчики (anti-drift)
- `delegations`: 38 (researcher; product-manager; brand-designer ×2; ux-analyst ×3; design-token-extractor; architect-claude; architect-codex; developer ×28 — M0.1–M0.6, M0.5-разведка, M1-вертикаль, окно-§3, обложка-движок, группировка, +2 фикса чипов, очередь-display, фикс converting-витрины, Status-каркас, фикс selfcheck_status, развязка self-check'ов, фикс раскладки Status, отмена-механика, отмена-UI+QueueView-фикс, надёжность-аудит, надёжность-фиксы, доводка-надёжности, нарезка-движок, нарезка-UI, упаковка-installer+DMG, Setup-экран-§01). NB: обложка-пикер НЕ делегировал (код был на диске, верифицировал). Несколько фоновых агентов оборвались по ПОТЕРЕ СВЯЗИ (закрытый ноут / 403 auth), НЕ по отмене человеком — каждый раз код приезжал на диск, Юрка сам прогонял self-check и чинил (урок памяти `interrupted-agent-not-user-cancel`).
- `yurka_direct_actions`: 3 — **срыв в ручной режим:** лепил макеты сам в чат-виджете на G2b → урок `.patches/001`. Впредь: правки макета через UXAnalyst, показ — только реальным рендером файла.
- **2026-07-07:** +≈7 делегаций (architect-claude ∥ architect-codex — авто-всплытие; developer ×2 — побудка агента + полировка UI; tech-writer — README RU/EN; brand-designer ×2 — соц-баннер + центровка) → **≈45 всего**. Точный ранний счёт этой сессии частично потерян при compaction. Прямых действий Юрки — **0 новых** (захват+кроп скриншотов и утилита `crop_window*.py` = визуальная верификация/подготовка ассетов, зона Юрки: только у него руки к живому окну, не продуктовый код; версии-строки = микроправки).
