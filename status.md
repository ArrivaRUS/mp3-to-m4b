# status.md — mp3-to-m4b (живой лог исполнения)

> Обновляется по ходу разработки. План — `plans.md`. Гейты — `test-plan.md`.
> **Обновлено:** 2026-07-07 (отгружен 0.6.2 — авто-всплытие окна + Cmd+Q/меню + подпись обложки).

## Где мы
**ФАЗА 4 (разработка) — старт.** Пройдены гейты: G0 (clone-go) · G1 (PRD) · G2a (лого) · G2b (макеты) ·
G3 (дизайн-спец) · **G4 (архитектура)**. Дизайн и план полностью готовы и приняты человеком.

## ✅ ОТГРУЖЕНО — `mp3-to-m4b-0.6.2.dmg` sha `eaf84cc0` (2026-07-07)
Заход: **авто-всплытие приложения на дроп** + Cmd+Q/меню + перенос подписи обложки. Всё сверено ВЖИВУЮ (изолированный агент+окно; реальный plist не тронут — sha совпал). Гейт `selfcheck_all` **16/16** (новый `nudge` 41/41).
- **Авто-всплытие (главное):** агент при появлении НОВОЙ pending-книги делает `open -b <bundleid>` (rising-edge + agent-owned `notified.json`). Триггер = **СОБЫТИЕ ДРОПА, НЕ content-новизна** (человек оспорил новизну — урок `.patches/004`): любой сознательный (пере)дроп поднимает окно. ДВА сигнала дропа: **inode+st_dev в `source_rev` v2** (копия Finder = новые inode, эмпирически подтверждено на iCloud-APFS: cp→новый ino, mv→тот же) + **presence-ledger «был→исчез→появился»** (Finder MOVE сохраняет inode → inode слеп; поймал Codex #2). Миграция v1→v2 тихая (`source_rev_migrated`, 0 окон на первом скане после апдейта). Живой e2e: свежий/редроп-копия/move-in → всплывает; лежит/логин → 0; миграция → 0. Приложение для авто-всплытия НЕ менялось (агент будит; app уже умеет landing→confirm + `bringWindowForward`). Двойной архитектор: Claude #1 (inode) ∥ Codex #2 (move-in edge → presence). Codex висел на stdin → память `codex-bridge-stdin-hang`.
- **Меню + Cmd+Q:** приложение на голом `NSApplication` без `NSApp.mainMenu` → Cmd+Q не закрывал, копипаст в полях не работал. Добавлен `installMainMenu()` (App/Правка/Окно). Память `native-macos-app-needs-main-menu`.
- **Подпись обложки:** «ИЗ ФАЙЛА/ИЗ СЕТИ» убрана с оверлея на картинке → в заголовок справа от «ОБЛОЖКА» (`coverCap`), картинка чистая. Остаток: мелкая текстовая «Из файла» под превью (лейбл ряда тумбов) — убрать по слову человека.
- ⏳ Не сделано (по слову человека): git-коммит / релиз на GitHub — внешний шаг, только по явному «да».

## ✅ ОТГРУЖЕНО — `mp3-to-m4b-0.6.1.dmg` sha `ec1f1886` (2026-07-01)
Заход правок из real-test. Версия 0.6→**0.6.1** (отличать от прошлой забагованной 0.6). Все правки СВЕРЕНЫ ВЖИВУЮ в окне 400pt (window-id, изолированный support с done-книгой; реальный launchd-plist не тронут — sha до/после совпал).
- **Футер-версия**: была захардкожена «0.1.0» в SetupView/QueueView/StatusView → привязана к `Tokens.appVersion`; agent `__version__`=0.6.1. (Ровно поэтому прошлый 0.6 показывал в футере 0.1.0 — правка версии не доезжала до хардкод-строк.)
- **Очередь → «Готово» (DoneRow)**: «Открыть» рвалась посреди слова + «Собрать заново» серая-«мёртвая». Фикс: `QButton` `.lineLimit(1)`+`.fixedSize` (лейблы не переносятся); «Собрать заново» → `brandCyan`; «Открыть» → новый `QIconButton` (SF Symbol `folder`, 30×30, `.help`/a11y «Открыть»).
- **Саблайн done**: «Готово · N глав · MM:SS» → «N глав · 25 ч 56 м» (убрано «Готово» — статус несёт галочка-диск; `QueueDuration.hoursMinutes`, без секунд; мёртвый `clock` удалён).
- **Гейт**: `selfcheck_all` **15/15** (505s: m1 58/58, fast 35/35, queue 18/18, reconvert 34/34, agent_update 12/12, …), `swiftc -typecheck` EXIT 0, DMG verify ok, старый 0.6 удалён.
**Диагностика real-test (НЕ баги кода):** «не стартует + показывает 1» = повторный дроп УЖЕ собранной книги (агент дедупит done по содержимому; «1» = прежняя сборка, счётчик за всё время). Пересбор — кнопкой «Собрать заново» (reconvert-путь цел, selfcheck 34/34). Частые `agent_started` = норма (launchd `WatchPaths`, не краш).

## 🔧 PENDING — правки из real-test (2026-06-30; человек ставит DMG и гоняет на СВОИХ книгах)
**Контекст:** M1 + упаковка ✅. DMG `build/dist/mp3-to-m4b-0.1.0.dmg` ставится и **РАБОТАЕТ** — служба сама нашла книгу «Рассечение Стоуна» (56 глав) и подняла окно подтверждения. Человек вычитывает экраны и даёт правки пачкой ⇒ собирать в один rebuild, не дёргать по одной.

**✅ ОТГРУЖЕН DMG `mp3-to-m4b-0.1.0.dmg` sha `2d933f34` (2026-07-01) — батчи 1-3 + P-PARAMS + прогресс + aac_at, ВСЁ СВЕРЕНО ВЖИВУЮ (window-id):**
- Окно подтверждения (shot_confirm.png): подпись **«Автор / чтец»** ✅, тумблер **«Как в источнике»** дефолт + хинт «· 48 кГц» ✅, нарезка слайдер 300/диапазон 250–700 ✅.
- Экран сборки (shot_converting.png): **детерминированный прогресс-бар** 43% + «43 %» ✅, «Глава 2 из 4: …» ✅, «Прошло 1:15 · Осталось ≈1:39» ✅, кнопка **«Отменить конвертацию»** ✅.
- Гейт `selfcheck_all` 11/11 (m1 58/58), `swiftc -typecheck` EXIT 0, app собран (universal+codesign), DMG verify ok.
- ⚠️ Для активации НОВОГО агента (P-PARAMS/прогресс/aac_at) человек ДОЛЖЕН переустановить: Setup → «Завершить установку» (агент копируется в App Support только при установке; иначе UI новый, агент старый → прогресс пустой).
- Предыдущий DMG `2d1c58d3` (только confirm-polish) — устарел.

**✅ СТРАНИЦА «Настройки» ГОТОВА (developer a2df, 2026-07-01):** `SettingsPlaceholderView`→`SettingsView` (main.swift ~219-640, call-site 154). Смена отслеживаемой папки через тот же бандловый `installer.sh` (re-point агента, переиспользован `InstallRunner`+`InstallPhase`+`FFmpegProbe`+folder-chooser из SetupView). Безопасность: `appliedDir` переключается ТОЛЬКО после installer exit 0; при сбое — старая папка + честная ошибка из stderr; кнопка активна только если папка иная И существует. Блок статуса ffmpeg+агент; «Параметры качества — скоро». Новый `selfcheck_installer_repoint` 13/13 (доказал: plist WatchPaths[0]+MP3TOM4B_WATCH_DIR переезжают на новую папку, идемпотентно). `selfcheck_all` **12/12**, typecheck EXIT 0. Реальный plist не тронут. **Визуальная сверка — в финальном проходе перед DMG.**

**СДЕЛАНО РАНЬШЕ (вошло в `2d1c58d3`, теперь и в `2d933f34`):** окно подтверждения — кап «КНИГА» убран, «Автор/Название» +1pt, «ГЛАВЫ» +1pt; текст Setup, папка честный статус + «Создать», «Готово к установке»/«Завершить установку».

**БАТЧ 1-3 ✅ НА ДИСКЕ (developer ae626 вернулся 2026-06-30; полный гейт `selfcheck_all` гоняется ПОСЛЕ инцидента с доступом — см. ниже; затем визуальная сверка + DMG):**
> Проверено разработчиком: `swiftc -typecheck` EXIT0, ffprobe-доказательство (scratchpad/sr_proof): источник 32кГц → выход 32кГц, явные 44.1/48 ресэмплят.
> **✅ Юрка прогнал полный гейт после восстановления доступа: `selfcheck_all` 11/11 ЗЕЛЁНЫЙ (367c) — m1 41/41 (+7 SR-проверок), reliability 16/16 (mixed 44100→48000), split 26/26, остальные зелёные.** Остаётся: `swift build` + ВИЗУАЛЬНАЯ сверка окна (подпись/тумблер/слайдер) — в финальном проходе ПОСЛЕ следующего батча (один билд + один DMG со всем).
1. **Частота из mp3** (не дефолт): сейчас жёсткий 44100 (`scan.DEFAULT_PARAMS.samplerate` + `build_m4b.DEFAULT_SAMPLERATE`); `probe.py` НЕ читает sample_rate. Дизайн: `probe.probe_file` читает `sample_rate` из streams (уже парсит их для cover) → `scan` пишет book-level `source_samplerate` = max читаемых SR (апсемпл меньшинства, без даунсемпла) рядом с `_total_duration_ms`; `DEFAULT_PARAMS.samplerate`=None («как в источнике»); `build_m4b._samplerate(params, manifest.source_samplerate)` — None→source, иначе явный; recipe (`aformat`/`-ar`) не трогать. UI (`main.swift` SegControl) +«Как в источнике» дефолт + хинт реального SR; `BookParams.samplerate`→Int?, +`sourceSamplerate`. Сверить, что confirm-build merge не затирает null→44100.
2. **Нарезка: минимум 250 МБ** (сейчас слайдер 100–700: `main.swift` splitThresholdMB + clamp `max(100,…)` + `SplitPlanner`/`StateModel`/agent-кламп если есть). Поднять нижнюю границу 100→**250** (дефолт 300 не трогать).
3. **Подпись поля автора → «Автор / чтец»** (решение человека 2026-06-30): поле берёт ID3 album_artist (часто это чтец), а не автор → честная подпись «Автор / чтец»; значение/биндинг без изменений (поле редактируемое — человек правит сам). ⟵ заменяет прежнюю идею «парсить автора из имени папки» (человек выбрал переименование подписи, не парсинг).

**🔴 P-PARAMS — ПОДТВЕРЖДЁННАЯ НАХОДКА (Юрка проверил `dispatcher.py` 2026-06-30):** НЕТ слияния command.params→manifest. `_apply_cover_choice` (dispatcher.py:194,310) применяет ТОЛЬКО `cover_id`/`cover_custom_path`; сборка читает `params = manifest.get("params")` (dispatcher.py:375) = дефолты сканера. ⇒ выбор пользователя в окне (частота 44.1/48, битрейт, каналы, И САМА нарезка split/threshold) **СЕГОДНЯ НЕ ДОХОДИТ до движка**. «Как в источнике» работает (через дефолт манифеста). Пред-существующий пробел, НЕ регресс. **HIGH PRIORITY — первым в след. батче:** добавить применение command.params→manifest перед сборкой (по аналогии с cover). Это ПРЕРЕКВИЗИТ для: нарезки (существующая фича!), SR-override, битрейт/каналы, И будущего тумблера build_mode (D15).

**🔧 БАТЧ P-PARAMS+прогресс+aac_at — ПРЕРВАН и ДОДЕЛЫВАЕТСЯ (developer #1 a155 умер на обрыве процесса приложения мид-ран 2026-06-30 ~23:45; #2 acf6 «доделать» запущен 2026-07-01):**
> **На диске (py_compile OK, агентская часть готова):** P-PARAMS (`dispatcher._apply_params_choice`+`_PARAM_KEYS_FROM_COMMAND`), `aac_at` (`build_m4b._encoder`/`_audio_encoder_args` CBR), прогресс-движок (`build_m4b` ~537 + `scan` `prior_progress`-презерв). **НЕ начато:** Swift UI (StateModel `struct Progress`, main.swift детерминированный бар + кнопка «Отменить конвертацию») + тесты для нового. Финиш-dev: проверить ПРОВОДКУ агентского кода (вызов P-PARAMS в `_real_build`; aac_at в обоих build-путях; `-progress` wiring + state-патч) → дописать UI+тесты → `selfcheck_all` зелёный. Затем Юрка: build + визуальная сверка окна + DMG.
> **ОБНОВЛЕНИЕ 2026-07-01 ~00:42:** finish-dev #2 acf6 тоже оборвался (3-й раз, process-exit), НО UI успел лечь: `StateModel.struct BuildProgress` (полный контракт + `fraction`) + книга `progress: BuildProgress?` ✅; `main.swift` футер converting с «Отменить конвертацию» (D13) + проводка прогресса ✅. **`swiftc -typecheck` ВСЕГО app = EXIT 0** (агент умер в валидной точке). Python py_compile OK. → Юрка гоняет `selfcheck_all` (b35yxk48i). Дальше recovery = ВЕРИФИКАЦИЯ (мои инструменты, не суб-агент): гейт → build → визуальная сверка окна → DMG. Остаток для dev (если гейт выявит): тесты нового поведения (P-PARAMS/прогресс/aac_at) могли не успеть.
> ⚠️ УРОК (память `claude-app-documents-access-drop-restart` + interrupted≠cancel): фоновые агенты ТРИЖДЫ погибли от выхода процесса приложения (TCC-лок → process-exit ×2). Митигация: держать app открытым; Edit-персистентность спасает частичную работу → recovery = «assess+finish», НЕ заново; фаза верификации/сборки идемпотентна (просто перезапустить). ПОТОМ отдельным батчем — Ступень 2 (параллельный «Быстрый режим» + тумблер D15).
0. **P-PARAMS фикс** (см. выше) — команда→манифест params перед сборкой. Пререквизит всего ниже.
4. **Живой прогресс сборки + «Отменить конвертацию» на экране сборки** (человек 2026-06-30: «не показывается прогресс бар, просто бегает полосочка»). Сейчас на converting — INDETERMINATE ProgressView (обманка). Надо determinate с: % + полоса, прошло (mm:ss), ETA, «Глава X из Y: <имя>», + кнопка отмены прямо здесь.
   - **Движок:** `build_m4b._run_ffmpeg` (Popen+poll для cancel уже есть) — добавить ffmpeg `-progress pipe:` + парс `out_time_us` (⚠️ `out_time_ms` в ffmpeg на самом деле МИКРОсекунды — баг совместимости; брать `out_time_us`/1000). frac = out_time_ms/total_duration_ms; ETA = elapsed*(1/frac−1) при frac>порога; current_chapter = первая глава, чей кумулятивный конец > out_time_ms. Работает и на concat-filter, и на demuxer пути (56 глав = demuxer — ОБЯЗАТЕЛЬНО проверить там).
   - **Состояние:** build-процесс (он уже пишет state.json через `refresh_showcase` на переходах) — throttle ~1.5s, targeted atomic patch `books[i].progress` (см. контракт ниже). `refresh_showcase` модифицировать: ПРЕЗЕРВИТЬ `.progress` для книги в converting (как презервит pending_groups), чтобы скан-рефреш не стирал. На done/error/cancel — progress уходит (статус≠converting).
   - **Контракт `state.json` book.progress (только при status==converting):** `{percent:0..100, out_time_ms:int, total_ms:int, elapsed_s:int, eta_s:int|null, current_chapter_index:int|null, current_chapter_name:str|null, total_chapters:int}`.
   - **UI (`main.swift` converting-экран):** determinate ProgressView(value: percent) + строки прошло/ETA/«Глава X из Y» + кнопка «Отменить конвертацию» → шлёт СУЩЕСТВУЮЩУЮ cancel-команду (как в QueueView, D13; converting→pending-confirm, file-watch вернёт окно в confirm). `StateModel`: +struct Progress.
   - **Self-check:** прогресс растёт (percent↑, chapter↑) до ~100→done на РЕАЛЬНОЙ многоглавой книге (demuxer путь); отмена с converting → pending-confirm.

**🏗️ ТРЕК АРХИТЕКТУРЫ — ускорение движка (запрос человека 2026-06-30):** двойной архитектор (Claude ∥ codex/GPT-5.5, READ-ONLY) прорабатывает СИЛЬНОЕ ускорение сборки. Узкое место: один ffmpeg ≈1 ядро на AAC-энкод. Кандидаты в брифе: (а) параллельный per-chapter энкод по всем ядрам + concat-demuxer **stream-copy** (без ре-энкода) → near-linear; (б) macOS `aac_at` (AudioToolbox) быстрее/качественнее встроенного `aac`; (в) комбинация. Инварианты: ffmpeg-only, валидный .m4b, бесшовность стыков/тайминги глав, отмена N детей, прогресс в новом конвейере = тот же контракт, уважать SR-из-источника, без регресса качества. Артефакты архитекторов → `arch/speedup-claude.md` / `arch/speedup-codex.md`. **Далее:** Юрка судит #1 vs #2 → синтез → ⛔ показать человеку рекомендацию (меняет движок + влияет на качество → нужен go) → внедрять ПОСЛЕ батчей 1-4 (overlap по `build_m4b.py`; прогресс-вычисление переписать под новый конвейер, UI/контракт не трогая).
  - **✅ Архитектор #2 (codex/GPT-5.5, xhigh, read-only) ВЕРНУЛСЯ** — `arch/speedup-codex.md`. Рекомендация GPT: НЕ чистый per-chapter (55 AAC-швов на 56 главах), а **параллельные ГРУППЫ подряд-глав (K≈workers) → финальный concat `-c copy`** (только workers−1 шов) + Apple **`aac_at` CBR 192k** + прогресс `-progress pipe:1`. Оба кодера однопоточны (`-threads` не помогает — только параллельные процессы). Милстоуны: M-A (`aac_at`+прогресс в текущем single-пути, ~1.3–3×, без риска швов) → M-B (инфра пула: гасить N детей, агрегат прогресса, cleanup temp-ДЕРЕВА) → M-C (группы→stream-copy, главы по ФАКТИЧЕСКОЙ длительности фрагментов из ffprobe — иначе дрейф от AAC frame-rounding) → M-D (QA швов + fallback на single-encode). Оценка на 56гл/26ч: вар1 ~1.3–3×, вар2@8w ~4–7× / @12-16w ~6–10×, вар3(per-chapter) ~8–15×. Риски: швы (главный→QA+fallback), дрейф глав (главы из факт. длительности), промеж. контейнер только .m4a/mp4 (не ADTS .aac), диск ×2.1–2.4, `out_time_ms` ffmpeg = микросекунды. NB: M-A пересекается с батчем-4 (прогресс) → при синтезе СЛИТЬ их (прогресс+aac_at одним шагом в single-пути).
  - **✅ Архитектор #1 (Claude) ВЕРНУЛСЯ** — `arch/speedup-claude.md`. Замерил: `-threads` аудио НЕ параллелит (12.77 vs 12.83с) → только процессы. `aac_at` ×1.24 даром/бесшовно. Параллель per-chapter ×6–8, НО ДОКАЗАЛ `silencedetect`: ~23мс тишины на КАЖДОМ стыке (ровно 180/360/540/720с) + дрейф меток до ~1.3с/56гл если из исходных длительностей (→ строить из probed). Альт: `aac_at` VBR single-pass ×2.35 бесшовно.
  - **✅ СИНТЕЗ Юрки → `arch/speedup-synthesis.md`.** #2 (группы подряд-глав → швов workers−1, не 55) улучшает #1; #1 эмпирически обосновал швы+дрейф.
  - **✅ РЕШЕНИЕ ЧЕЛОВЕКА (2026-06-30, D15): ДВА режима + переключатель в UI** — «Быстрый» (параллельные группы→`-c copy`, ×6–10, ~25мс на стыках=границах глав, default) / «Бесшовный» (single-pass, бит-в-бит). Фундамент: `aac_at`+прогресс (Ступень 1, бесшовно). Страховка «Быстрого»: метки из probed-длительности, QA швов, авто-fallback. `build()` ветвит по `params.build_mode` (`fast`/`seamless`). Внедрять ПОСЛЕ батчей 1-4 (overlap по `build_m4b.py`).

**✅ КЛИППИНГ ПОЧИНЕН (developer a8fa, 2026-07-01) — ВИЗУАЛЬНАЯ СВЕРКА ЮРКОЙ В ПРОЦЕССЕ:** КОРЕНЬ (не то, что писали старые комменты про ScrollView под-репорт): `refitWindowHeight` (main.swift ~3333) присваивал окну высоту КОНТЕНТА вместо высоты РАМКИ, а рамка = контент + титлбар (32pt) → постоянный обрез на 32pt при ЛЮБОМ числе книг, общий для Status и Queue (одно окно). Фикс: content→frame через `window.frameRect(forContentRect:)` перед сравнением/установкой (один фикс на оба экрана); `cappedContentHeight` (~3355) выводит потолок из реального хром-инсета (`visibleFrame`→frameRect/contentRect), убран хардкод титлбара. `StatusView.orderedRecent` prefix 6→8 (показывает 5, дальше скролл). `Tokens.recentListMax`=264 НЕ менялся (вмещает 5). `selfcheck_all` **12/12**, typecheck 0; dev показал CLIP=0 офф-скрин на Status n=1/5/6 и Queue done=5/8. **✅ ЮРКА СВЕРИЛ ЖИВЬЁМ (window-id): Status n=5 (все 5 книг + футер + копирайт видны, 0 клипа) и n=1 (строка целая, окно ужалось под 1 книгу) — shot_status5.png/shot_status1.png.** Queue/Settings живьём НЕ снимал (нужен клик-навигация) — доверие по общей починке окна + гейт; человек глянет при установке.

**✅✅ ОТГРУЖЕН ОБЩИЙ DMG `mp3-to-m4b-0.1.0.dmg` sha `7df5a162` (2026-07-01, 09:02):** прогресс-бар+отмена + Настройки + клиппинг+5 + частота-из-источника + нарезка-250 + «Автор/чтец» + P-PARAMS + aac_at. Образ verify ok. Предыдущий `2d933f34` устарел. Человеку: install → Setup→«Завершить установку» (обновить агент, иначе новый движок/прогресс не активны). Осталось на потом: «Собрать заново» (reconvert), «Быстрый режим» (Ступень 2, ×6–10).

**✅ ПОЧИНЕНО (developer af95, 2026-07-01): окно подтверждения ВЫШЕ ЭКРАНА → «Собрать» недостижима** (блокировало сборку на 56-главой книге). Фикс: ConfirmView = `[фикс header]+[один внешний ScrollView с телом обе колонки]+[ЗАКРЕПЛЁННАЯ панель Пропустить/Позже/Собрать]`, вложенный скролл глав убран, окно капается по `visibleFrame`. **Юрка сверил ВЖИВУЮ на реальной 56-главой книге: winHeight=887 ≤ visibleFrame=896 (влезает!), «Собрать» закреплена+видна, тело скроллится** (shot_confirm56.png + winfit-замер). Гейт 12/12. ⚠️ УРОК записан (`window-id-screenshot-misses-screen-overflow`): window-id снимает ВСЁ окно независимо от экрана → screen-overflow ловится только замером winHeight vs visibleFrame, не картинкой.

**✅ ГОТОВ ДИЗАЙН DMG-инсталлятора (developer a8d2, 2026-07-01):** порт fb2 (`dmgbuild` + `dmg-settings.py` + `branding/dmg-background.svg/png/@2x`) в НАШИ цвета (cyan→teal→indigo) + наш знак (книга+play) + вордмарк «mp3 → m4b». `build/make-dmg.sh` (новый, dmgbuild из build/.venv), `build/build-dmg.sh` → fallback. **Юрка смонтировал + сверил ВЖИВУЮ окно DMG (shot_dmg_0.png): фон+знак+вордмарк, слева mp3-to-m4b.app → стрелка → Applications, drag-hint, инструкция первого запуска — всё в нашем бренде.**

**✅✅✅ ОТГРУЖЕН БРЕНДИРОВАННЫЙ DMG `438bf518` (2026-07-01, 09:59):** фикс окна подтверждения (Собрать достижима) + брендированный инсталлятор + ВСЁ прежнее (компактная главная, Настройки, прогресс, частота-из-источника, нарезка, «Автор/чтец», P-PARAMS, aac_at, Очистить/Сброс). Собран через `make-dmg.sh` (dmgbuild), verify ok, окно смонтированного DMG сверено. Предыдущий `a9a97063` устарел. **ВАЖНО человеку: install → Setup→«Завершить установку» (обновить агент — иначе новые движковые фичи/прогресс/SR-хинт не активны).** Осталось: reconvert («Собрать заново»), «Быстрый режим» (Ступень 2 ×6–10).

**✅ ШАПКА DMG ИСПРАВЛЕНА (a8d2, 2026-07-01):** иконка `scale 0.42→0.31 translate(338,49)` — высота 25px ≈ текст 26px, y-центры 58.5≈59 (вровень со строкой); стрелка пересчитана по реальным ink-границам (mp3 x=436, m4b x=472, центр 454) → зазоры 13/12px, по центру. Пере-рендер png/@2x, DMG пересобран (промежуточный sha 338ecb40 — не финал). **Юрка сверил обновлённый `dmg-background.png` вживую: «📘 mp3 › m4b» единой ровной строкой, стрелка по центру.** Финальный DMG соберётся после Быстрого режима + reconvert.

**✅ «БЫСТРЫЙ РЕЖИМ» ГОТОВ (developer a06f, 2026-07-01, D15):** параллельные группы подряд-глав → concat stream-copy + тумблер «Быстрый/Бесшовный» (default Быстрый). `build_m4b`: `_plan_encode_groups`/`_run_group_pool`/`_build_with_parallel_groups` (2-проходная склейка: concat→probe→snap END→remux, дрейф 0мс), `_terminate_ffmpeg_many` (гасит N детей), `_FastPathUnusable`→fallback single. `build_mode` в scan/StateModel/EngineClient/main.swift(тумблер). `selfcheck_all` **13/13** (нов. `selfcheck_fast` 35/35), typecheck 0. Доказано ffprobe: fast .m4b 6гл, метки из probed-длит., END==container (0мс), aac_at 48кГц, обложка, faststart; скорость ×3.78 на 16 ядрах (на быстрых тестах упор в накладные; ×6-10 когда доминирует энкод — реальные книги; параллелизм линеен); отмена 6→0 детей; fallback ок. **Юрка сверит вживую: тумблер + реальная сборка в Быстром + скорость.** ⚠️ FAST_MAX_WORKERS=8.

**✅ «СОБРАТЬ ЗАНОВО» (reconvert) ГОТОВ (developer ae63, 2026-07-01):** команда `reconvert` (dispatcher `_handle_reconvert` ~721): re-arm done→pending-confirm + свежий confirm_token + **очистка `processed_keys`** (иначе повторная сборка при неизменном source_rev дедупнулась бы) + сброс result/error/build/progress; reject при missing-manifest/source-gone/non-done. Кнопка «Собрать заново» (link-стиль, QueueView секция ГОТОВО) + `EngineClient.ReconvertCommand`. `selfcheck_reconvert` 24/24 (доказано: после reconvert повторный confirm-build доходит до build_done, не idempotent_skip). `selfcheck_all` **14/14**, typecheck 0. NB: кнопки не было в макете 07-queue.html → выбран link-стиль по брифу; Юрка сверит вживую.
**→ ВСЕ запрошенные фичи готовы.**

**✅✅✅ ОТГРУЖЕН ФИНАЛЬНЫЙ DMG `0815f315` (2026-07-01, 11:08):** Быстрый режим+тумблер + reconvert + фикс окна подтверждения + брендированный установщик (шапка со стрелкой +3px) + ВСЁ прежнее. Юрка: независимый гейт `selfcheck_all` **14/14** (fast 35 + reconvert 24 + все), typecheck 0; визуально сверено вживую (window-id): тумблер «Быстрый/Бесшовный» default Быстрый в КАЧЕСТВО, окно 56гл влезает (887≤896, «Собрать» закреплена), шапка DMG (иконка вровень, стрелка по центру). Предыдущие DMG устарели. Человеку: install → Setup→«Завершить установку».
**Мелочи-долги (не блокеры):** (1) подсказка тумблера обрезается «…на стыках гл…» → укоротить/перенос; (2) кнопка «Собрать заново» — link-стиль, не было в макете 07-queue.html (норм).

**🔴🔴 КОРНЕВАЯ НАХОДКА (real-test 2026-07-01): АГЕНТ У ЧЕЛОВЕКА НИКОГДА НЕ ОБНОВЛЯЛСЯ.** Проверено на живой установке: staged `~/Library/Application Support/mp3-to-m4b/bin/agent/build_m4b.py` = 30.06 21:56, НОЛЬ нового кода (grep aac_at/parallel/build_mode = 0). Человек ставил свежие DMG (новый app UI), но АГЕНТ не переустанавливался → все движковые фичи (прогресс, Быстрый режим, aac_at, SR-из-источника, reconvert) СПЯТ. Живая сборка шла старым путём (`-c:a aac -ar 44100` одно-проходно, pgrep ffmpeg подтвердил) → «Запуск…» навсегда (старый агент не пишет progress). ПРИЧИНА: `isAgentInstalled()`==true(старый) → Setup пропускается → нет GUI-пути обновить агент. Это объясняет ВСЕ прошлые «фича X не работает» этой сессии. Урок в память: `app-update-doesnt-update-staged-agent`.
**✅ ФИКС АВТО-ОБНОВЛЕНИЯ АГЕНТА ГОТОВ (developer a9e63, 2026-07-01):** при запуске app сравнивает хэш встроенного (Contents/Resources/agent) vs staged; расхождение → авто-запуск бандлового installer.sh (`AgentUpdatingView` индикатор, ТЕКУЩАЯ watch-папка) + `AgentFreshness` + кнопка «Обновить агент» в Настройках; dev-запуск без бандла → `.undecidable` (не трогает). NB: a9e63 завершил ход ДО подтверждения своего self-check (оставил его в фоне bx1dl7q4y + ждуна). **Юрка проверил: `swiftc -typecheck app/*.swift` (все 8) EXIT 0**; Python-suite `selfcheck_agent_update` зарегистрирован — подтвержу финальным гейтом. Затем: человек ставит DMG ОДИН раз → app сам обновит агент → fast/прогресс/reconvert оживут. NB: человеку сказано отменить старую сборку.

**📌 ВЕРСИЯ → 0.6 (запрос человека 2026-07-01): применить АТОМАРНО в финальной сборке** (после a83f, чтобы не столкнуться по Tokens.swift): `build/build-app.sh:29` VERSION default `0.1.0`→`0.6`; `build/make-dmg.sh:38` default `0.1.0`→`0.6`; `app/Tokens.swift:60` `appVersion="0.1.0"`→`"0.6"`. Затем build 0.6 (Info.plist CFBundleShortVersion/Version) + `make-dmg.sh 0.6` → `mp3-to-m4b-0.6.dmg`.

**🔧 ГЛАВЫ-В-ОКОШКЕ — ДОРАБАТЫВАЕТСЯ (a83f resumed, 2026-07-01):** 1-я версия a83f пере-скорректировала: дала ПРАВОЙ колонке тоже `maxHeight: chapterAreaCap` → её нижние контролы (Стерео/Моно, «Как в источнике», тумблер Быстрый/Бесшовный, Нарезка, размер) ОБРЕЗАНЫ (Юрка сверил на 56гл, shot_confirm56d.png — видно только обложку+битрейт). Поправка внесена (a83f v2): body без внешнего скролла, `chapterList` maxHeight:.infinity (заполняет+скроллит), правой колонке safeguard maxHeight:bodyAreaCap. **Юрка сверил на 56гл (shot_confirm56e): главы в окошке ✅, header/«Собрать» закреплены ✅, SR-хинт «Как в источнике · 48 кГц» РАБОТАЕТ ✅. НО правая колонка ВСЁ ЕЩЁ выше экрана 896: НАРЕЗКА+размер обрезаны снизу** (тумблер «Быстрый/Бесшовный» выдавил). → **✅ КОМПАКЦИЯ ГОТОВА (developer aed4, 2026-07-01):** обложка 242→**160** (`Tokens.M.confirmCoverMax`), миниатюры 54→46, поджаты отступы КАЧЕСТВА/секций/estimate; подсказка тумблера укорочена (без обрезки). ≈**123pt** сэкономлено (target был 150-180 → на 896px низ может быть впритык; запас: `confirmCoverMax` 160→150 = ещё ~10pt). typecheck 0, `selfcheck_all` 15/15. **✅✅✅ ОТГРУЖЕН ФИНАЛ `mp3-to-m4b-0.6.dmg` sha `3daee938` (2026-07-01, 12:45):** версия 0.6, единственный DMG (0.1.0 удалён). Юрка сверил на 56гл ВЖИВУЮ (shot_confirm56f): правая колонка ЦЕЛИКОМ (обложка 160 + битрейт + стерео + «· 48 кГц» + «Быстрый/Бесшовный» + полная подсказка + НАРЕЗКА + «≈2.24ГБ» размер), главы в окошке, «Собрать» закреплена, окно 888≤896. Независимый гейт `selfcheck_all` **15/15** (agent_update 12 + reconvert 34 + fast 35 + все). Авто-обновление агента сверено вживую (экран «Обновляю фоновый агент…»). Install: DMG→Программы→открыть→агент сам обновится→конвертить (новая книга или «Собрать заново»). ВСЕ запросы сессии закрыты.

**✅ RECONVERT ПЕРЕСКАНИРУЕТ КНИГУ ГОТОВ (developer a820, 2026-07-01):** `_handle_reconvert`→`scan.rescan_book_manifest(force=True)` заново probe'ит главы/теги/`source_samplerate`/обложку, свежий source_rev+confirm_token, processed_keys=[]. Старая книга (без source_samplerate) после reconvert получает его → SR-хинт + новые поля оживают БЕЗ переименования. `selfcheck_reconvert` 34/34, `selfcheck_all` **15/15**. (Ранее в работе:)** reconvert ПЕРЕСКАНИРУЕТ книгу (re-probe)** (real-test: SR-хинт «Как в источнике · N кГц» не показывается — тот же корень, старый агент не записал source_samplerate; у КЭШИРОВАННОЙ книги даже после авто-обновления агента поле не появится без re-scan). Фикс: `_handle_reconvert` не просто re-arm, а пере-собирает манифест из текущих исходников (probe глав/тегов/source_samplerate/обложки, свежий source_rev) → pending-confirm. Тогда «Собрать заново» на старой книге оживляет SR-хинт + все новые поля БЕЗ переименования папки. dispatcher.py (+scan reuse) — не конфликтует с a83f (main.swift). Self-check: старый манифест без source_samplerate → после reconvert имеет его. **Итог: ВСЕ симптомы старого агента (прогресс, Быстрый, SR-хинт) закрываются авто-обновлением агента + reconvert-reprobe.** **✅ ВЕРСИЯ 0.6 применена** (build-app.sh:29, make-dmg.sh:38, Tokens.appVersion). Сейчас: build 0.6 (bzs1argoe) → мой гейт → визуальная сверка скролла 56гл → `mp3-to-m4b-0.6.dmg`. Моя пере-коррекция в af95: сделал скролл ВСЕГО body2col → правая колонка (качество/обложка/режим/нарезка) уезжает вверх при листании 56 глав. Надо (как D10 «главы на всю высоту окна»): убрать внешний body-ScrollView; главы — в bounded ScrollView ДЕТЕРМИНИРОВАННОЙ высоты (скролл внутри), правая колонка — на месте/видима; header+панель «Собрать» закреплены; окно ≤ visibleFrame. Листается ТОЛЬКО область глав, окно не растёт от числа глав. Если правая колонка сама выше экрана (мелкий экран/много контролов) — ей тоже внутренний скролл (safeguard); БЕЗ GeometryReader-высоты (.patches/002). Юрка сверит на 56гл: главы скроллятся в окошке, правая колонка видна, «Собрать» закреплена, окно ≤ экрана.

**✅ СТРЕЛКА В ШАПКЕ DMG — оптический сдвиг +3px вправо (a8d2, 2026-07-01):** глиф «›» раскрыт влево → при пиксельном центре (13/13) читался ближе к mp3; сдвинут +3px (bbox 16/10, оптически по центру). Юрка сверил кроп + открыл человеку; ждёт подтверждения. DMG промежуточный sha `b86cf61f` (не финал).

(reconvert — см. блок выше, в работе ae63)

**✅ ГОТОВО + ОТГРУЖЕН DMG `a9a97063` (2026-07-01, 09:33): реорганизация UI по образцу соседа fb2-to-epub**
> Сверено Юркой вживую (window-id): компактная главная на 5 и 1 книге (shot_c5.png/shot_c1.png) — 2 стата в hero (СОБРАНО/ЗА СЕГОДНЯ), папка+FFMPEG убраны с главной, «Очередь» компактной строкой, «🗑 Очистить» на списке, клиппинга нет, футер целый. Настройки (movrouter+движок+Сбросить статистику+FDA+версия) — гейт+typecheck, живьём НЕ снимал (нужен клик), человек глянет. `selfcheck_all` 12/12, typecheck 0. Новый `app/EngineClient+Status.swift` (app-маркеры clear/reset, state.json не трогается) + вписан в build-app.sh SWIFT_SRCS. Предыдущий DMG `7df5a162` устарел.
 (запрос человека: настройки→страница Настроек, компактная статистика, кнопка «Очистить» лога). Порт проверенного fb2: `fb2/app/StatusView.swift` (компактный hero: бейдж агента + кольцо + 2 стата стопкой; папка убрана с главной; «Очистить» на списке), `fb2/app/SettingsView.swift` (папка+движок+«Сбросить статистику»·FDA+Версия), `fb2/app/EngineClient+Status.swift` (**КЛЮЧ: clear/reset через app-МАРКЕРЫ `recent-cleared-at`+baseline, БЕЗ записи state.json** — та же архитектура). Наши адаптации: терминология «собрано»; FFMPEG-карточка с главной→в Настройки; **«Очередь подтверждения» ОСТАВЛЕНА на главной** (наша ключевая функция, не настройка — у fb2 очереди нет); Settings живёт в main.swift (~219-640, не отдельный файл); нет UpdateChecker → версия+GitHub-ссылка. СОХРАНИТЬ клиппинг-фикс (refitWindowHeight). Когда вернётся → Юрка: build + визуальная сверка (компактная главная 1/5 книг + Настройки + Очистить/Сброс) + DMG.

**ℹ️ UX-разъяснение (не баг, дано человеку 2026-07-01):** «ничего не конвертируется» = в watched-папке только УЖЕ собранная книга (Рассечение Стоуна done, `book_id`=sha256(путь), `source_rev` не менялся); агент идемпотентно не пересобирает. Обходной путь дан человеку: переименовать папку (новый book_id → новая книга → окно подтверждения → «Собрать»). Агент у человека ещё СТАРЫЙ (bin 30.06) → новые фичи после Setup→«Завершить установку» с новым DMG. РЕШЕНО: собрать ОДИН DMG (прогресс+Настройки+клиппинг+reconvert) → одна установка.

**🔧 UX-ФИЧА «Собрать заново» (real-test 2026-07-01, человек застрял на пересборке готовой книги):** на готовых книгах (раздел «Готово» в QueueView + возможно строка на Status) кнопка «Собрать заново» → новая команда агенту `reconvert` (re-arm: done→pending-confirm, свежий confirm_token, при неизменном source_rev). Мотивация: пользователь удалил .m4b / хочет пересобрать — сейчас единственный путь переименовать папку (неочевидно). Требует: новая команда в dispatcher (валидация как cancel — таргетит book_id, без source_rev/token гонки) + UI-кнопка + self-check. Кандидат в батч клиппинга (общий QueueView) ИЛИ отдельный — решить по объёму. Не забыть: если .m4b отсутствует, «Собрать заново» тем более уместна.

**ПРОВЕРИТЬ у человека (по коду НЕ баг):** пустая «Отслеживаемая папка» на Status — данные+декодер верны (state.json имеет `watch_dir`, `AgentInfo` декодит без throw), вероятно транзиент при установке. Если человек подтвердит «всё ещё пусто» → баг обновления экрана, лезть в refresh.

**Как тестирует человек:** DMG → перетащить в Программы → правый клик «Открыть» → Setup → «Завершить установку» → Full Disk Access на `~/Library/Application Support/mp3-to-m4b/bin/runner.sh` (папка на Desktop) → дроп книги → окно подтверждения. Реальную установку (launchd) делает ТОЛЬКО человек; Юрка установщик не запускает (persistent config).

## Сделано
- Интейк, публичный репо, тех-разведка (ffmpeg-only).
- PRD/истории/беклог (MVP=26 Must). Лого + лого-пак. Макеты 7 экранов + флоу. Токены (~210) + дизайн-спец.
- Двойной архитектор + синтез (G4): протокол command-log/манифесты, Python-движок, обложки Pillow.
- execution-pack собран (`plans.md`/`status.md`/`test-plan.md`). Контрольный коммит запушен.
- **M0.1 ✅** каркас репо: agent-пакет (alive, 8 каталогов данных) + `bin/runner.sh` + пустое тёмное окно `app/` + `build/build-app.sh` (universal `.app`, codesign strict verify ok). Проверено Юркой.
- **M0.2 ✅** агент: скан → манифест `queue/books/<id>.json` (`pending-confirm`+`source_rev`+`confirm_token`, главы натур.сортировки, дефолты D2/D6) + витрина `state.json` (агент — единственный писатель). Проверено Юркой: идемпотентность + re-arm на новый файл + игнор мусора. `scan.py`/`metadata.py`(natural_sort+chapter_name)/`__main__.py --scan`.

- **M0.3 ✅** приложение читает `state`+манифесты, показывает книгу+главы, всплывает на rising-edge. `StateModel.swift` (Codable + partial-read защита), `main.swift` (ReaderModel/ConfirmView/file-watch DispatchSource). **Проверено Юркой ВИЗУАЛЬНО (computer-use):** окно «Подтверждение книги» → «Толстой - Война и мир» → 4 главы (имена очищены, натур.порядок) → «Собрать». Developer по пути починил macOS-12 API при таргете 11.0 + гонку codesign под iCloud (build в staging вне облака).
- **M0.4 ✅** кнопка «Собрать» пишет `queue/commands/<cmd_id>.json` (`confirm-build`+8 полей, атомарно) + UI-ack «Отправлено…». `EngineClient.swift`. **Проверено Юркой настоящим кликом (computer-use):** файл команды появился, `confirm_token`/`source_rev` совпали с манифестом, `idempotency_key` детерминированный, без `.tmp`, `bad/` пуст.
- **M0.5 ✅** агент дренирует `queue/commands/`, валидирует, **fake-engine → `done`** + `state.json`, удаляет команду, события. `dispatcher.py` (drain/validate/_fake_build/quarantine), `state.append_event`. **Проверено Юркой ВЖИВУЮ сквозь весь круг (computer-use):** клик «Собрать» → команда → агент-drain → книга `done` (`result.fake`) → команда удалена → **окно само ушло в «Очередь пуста»** (file-watch). Протокол подтверждения доказан end-to-end. (NB: `events.jsonl` в боевом прогоне вышел пустым — проверить в M0.6.)
- **M0.6 ✅** защиты протокола: stale→`confirm_rejected_stale`, дедуп `idempotency_key` (двойной клик=один build), рестарт в `converting`→`error: interrupted`+cleanup temp, malformed→`bad/`, `events.jsonl` durable (fsync). **Проверено Юркой: `python3 -m agent.selfcheck_m0` → 36/36 PASS** → гейт `test-plan §M0` ЗАКРЫТ.

## ✅ M0 ЗАВЕРШЁН (скелет протокола, fake-engine)
Протокол app↔agent доказан **end-to-end вживую** (клик→команда→исполнение→окно ушло) + **36/36** защит (I1/I2/G5, stale/дедуп/interrupted/malformed, журнал-гейт «нет build_started без confirm_accepted», единственный писатель). Код M0 закоммичен.
**Следующий: M0.5 — реальная разведка** (ffprobe: длительности глав, ID3 автор/название, детект встроенной обложки) → затем **M1** (реальный `.m4b`).

## Открытые хвосты / к ратификации позже (QA)
- `stik=Audiobook` для Apple Books — проверить на QA, иначе опц. AtomicParsley.
- Кодек `aac` vs Apple `aac_at` — опц. сравнение QA.
- Долг косметики: brand-basics 2-я стопа `#0E1A22`→`#0E1822` (spec §0.2).
- **Иконка к релизу:** сейчас `.icns` собирается через `sips` (cairosvg нет); перед релизом запинить `cairosvg` в `build/.venv` для качественного `.icns` (пометка в `build-app.sh`).
- **codesign detritus** (iCloud FinderInfo-гонка, урок соседа .patches/003): сборочный ретрай гасит; отдельный пост-`codesign --verify` может косметически ругаться `detritus not allowed` — на запуск не влияет.

## Текущий милстоун
**M0.5 ✅** (разведка ffprobe, selfcheck 23/23) · **M1-вертикаль ✅** — реальный `.m4b` (selfcheck 34/34 + Юрка вскрыл боевую книгу: 5 глав 1/1000, обложка mjpeg attached_pic, brand M4A, +faststart, 44100/стерео, без дрейфа, I1, атомарность, идемпотентность; движок пишет `genre=Audiobook`, `stik` — открытый QA-вопрос). Движок сборки боевой.
**Окно подтверждения §3 ✅** — полное окно, 4 состояния сверены Юркой вживую (window-id): pending = ref03; converting/error стили ок, кнопки действий отложены (D13), warn-копирайт честный. Пересобрано чисто.
**Цепочка обложки — ПОЛНОСТЬЮ ✅** (движок + пикер + выбор→сборка):
- движок `cover.py` (Pillow-генерация квадратных вариантов с кириллицей + play-символом + best-effort веб DuckDuckGo/Yandex; манифест несёт `cover_options`/`cover_selected`; §cover 17/17).
- пикер в окне (`main.swift`: лента превью + большой превью выбранной + бейдж источника + «Заменить» через `NSOpenPanel`; «Искать в сети» отложено, D13). Выбор едет в `confirm-build` params (`cover_id`/`cover_custom_path`) — **БЕЗ отдельной команды cover-choice**; свою картинку **агент** копирует в `covers/` (app не пишет туда). `build_m4b.resolve_cover_path`: выбранная→встроенная→первая генерация, всегда `attached_pic`.
- **доказано: selfcheck_cover_pick 16/16** — вшивается ИМЕННО выбранная обложка (pixel-dist 0.6 к выбранной vs 19.4 к дефолтной; своя картинка ≈magenta dist 0.7; PRD-G4 «всегда обложка» оффлайн). Регрессии зелёные: M0 36/36 · M0.5 23/23 · M1 34/34 · cover 17/17.
- **Юрка сверил вживую** (window-id, книга «Каштанка»): пикер рисует 5 вариантов (встроенная выбрана + 4 генерации), большой превью, «Заменить»; тап-таргет — `Button`+`.contentShape(Rectangle())` (мой урок про мёртвые зоны применён). Показал человеку (Safari) — дизайн принят.
- NB: код пикера приехал на диск ещё до прерывания прошлой делегации — Юрка не переделывал, а **верифицировал** (selfcheck + живой рендер + чтение кода).

**Группировка ✅** (одиночные mp3 в КОРНЕ → sheet D1 → команда `grouping-choice` → агент материализует книги):
- scan детектит loose-mp3 в корне → `pending_groups` в state (group_id+rev+token, файлы, счёт, длительность; `grouping_processed`-ledger гасит повтор-промпт). Подпапки-книги работают как раньше.
- sheet (`GroupingSheet`/`FileChipsFlow` в main.swift): «Объединить в одну книгу» / «Собрать по отдельности» + чипы файлов + «Отмена/Продолжить». `EngineClient` пишет `grouping-choice`; `dispatcher` валидирует (token/rev/дедуп) и материализует: combine→1 книга N глав, separate→N книг.
- **доказано: selfcheck_grouping 22/22** (combine/separate, нат.сорт 1·2·10, forged-token/unknown/stale отклонены, идемпотентность, сосуществование с папками, PRD-G4 обложка) + регрессии зелёные.
- **БАГ вёрстки пойман Юркой вживую** (window-id): чипы наезжали на шапку (file-strip ~0 высоты). 1-й слепой фикс не сработал (`ZStack`+`alignmentGuide` не самосайзится по высоте). 2-й фикс — детерминированный пакинг (`NSAttributedString.size`→`VStack`/`HStack`, без GeometryReader-высоты) — **сверено: наложения нет, читаемо**. Урок `.patches/002`.

**Очередь (display) ✅** (секции по статусам + batch-чип + «Позже в очередь» → QueueView):
- `QueueView.swift`: 4 секции (ОЖИДАЕТ/В РАБОТЕ/ГОТОВО/ОШИБКА), qrow (обложка-38 из манифеста, имя, sub, qbtn), batch-чип, empty-state. Навигация: «Позже в очередь» → `.queue` (книга остаётся pending). Раскладка детерминированная (урок .patches/002 учтён).
- qbtn: pending→окно подтверждения · done→«Открыть» (reveal в Finder) · converting/error→неактивны (отмена/ретрай — отдельные слои, D13).
- **БАГ найден Юркой (self-check вскрыл):** секция «В РАБОТЕ» не наполнялась — диспетчер при `converting` обновлял только манифест, а не витрину `state.json` (очередь секционирует по витрине). + сам `selfcheck_queue` был ложно-зелёным (exit 0 при 3 FAIL). **Фикс:** `scan.refresh_showcase()` дёргается диспетчером на ВСЕХ переходах (converting/done/error) → витрина живая; exit self-check честный.
- **Проверено:** `selfcheck_queue` 18/18 (все 4 статуса в витрине, включая converting); регрессии после фикса зелёные — M1 34/34 · cover-pick 16/16 · grouping 22/22 (+ m0 36/36 · m05 23/23 · cover 17/17). NB: живой снимок самого экрана очереди не снял — Telegram человека постоянно перехватывал фокус окна; экран доказан self-check'ом, не скриншотом.

**Status (spec §5) — каркас ✅, добивается:**
- `StatusView.swift`: hero-card (кольцо + «АКТИВНО» + папка), 3 стат-карты (Собрано/За сегодня/ffmpeg — агент проецирует `totals.built/today` + `engine` версию), строки (агент/папка+Сменить/вход в очередь), футер, кредит. Навигация: Status — «домашний» (main.swift `screen=.status`; confirm/queue достижимы). Расхождение спека↔реальность: по-чаптерного «глава 7 из 12» НЕТ (сборка = один ffmpeg concat) → показываем «Сейчас: <книга>» + общий прогресс.
- `selfcheck_status` **21/21** (Юрка вскрыл self-check'ом ложный форж группировки без файлов → агент починил ТЕСТ реалистично, продакшн `refresh_showcase` доказан верным изоляцией).
- ✅ **наезд на футер починен** (вынес фикс-обвязку из скролла; список «последних книг» — height-capped скролл `recentListMax=264` НАД футером; убран Spacer). Бонус: кольцо на простое показывает `totals.built` + «всего». **Юрка сверил вживую на 6 книгах** (window-id): список скроллится в своей области, футер виден и кликабелен, наезда нет. `selfcheck_all` 8/8.
- ⚠️ **известный долг (developer нашёл):** тот же паттерн `VStack{header;ScrollView;footer}` без cap есть в `QueueView.swift` — при длинной очереди возможен такой же наезд. Слой `cancel` всё равно трогает QueueView → починить там же тем же приёмом.

**⚡ Развязка self-check'ов ✅** (корень тормозов всей сессии): вложенные регрессии (status→queue→grouping→… мультипликативно, ~30 мин) убраны — каждый сьют гоняет ТОЛЬКО свои проверки, общий плоский `agent/selfcheck_all.py` проходит все 8 по разу. **Юрка подтвердил: `selfcheck_all` 8/8 за 118s** (было ~30 мин). Продакшн не тронут.

**Отмена сборки (D13) ✅** — кооперативная, единственный владелец:
- `build_m4b`: ffmpeg через `Popen`+опрос (`_cancel_requested(book_id)`), `_terminate_ffmpeg` (SIGTERM→SIGKILL, reap child), `BuildCancelled`; argv/атомарность не тронуты (обычная сборка идентична). `dispatcher._real_build` ловит → `converting`→`pending-confirm` (rev/token целы, можно пересобрать) + сам потребляет cancel-команду; `drain` не трогает живую отмену (нет двойной обработки).
- `QueueView`: danger-кнопка «Отмена» на converting-строках → `EngineClient` пишет `{action:"cancel",book_id,idempotency_key,ts}`. + наезд футера в QueueView ПОЧИНЕН (cap `queueListMax=460`+скролл, как в Status).
- **Доказано: `selfcheck_cancel` 16/16** (ffmpeg реально мёртв — нет осиротыша; нет частичного `.m4b`; идемпотентность; cancel_moot для не-converting; пересборка после отмены). `selfcheck_all` **9/9**. **Юрка сверил вживую** (window-id, 9 книг во всех секциях): danger-кнопка «Отмена» на «В РАБОТЕ», клик пишет верную cancel-команду (shape совпал), наезда футера нет.
- ⚠️ мелкая косметика: «Отмена» переносится по слогам («Отмен/а») — кнопка узковата (фикс `.lineLimit(1)`/шире — в полировку).

**Надёжность (E1–E18) ✅** — аудит + точечные фиксы:
- Аудит `selfcheck_reliability` показал **12 уже-обработанных** (нет тегов, разные SR/каналы, оффлайн→обложка, идемпотентность, отмена, длинная книга→demuxer, мусор, недопустимые символы, один mp3, дубли глав, исчез источник→error, краш→cleanup) + 3 реальных пробела + 1 DEFER.
- Починены 3 пробела (агент-сторона, прод E3/E5/E10): **E3** битый mp3 → ВСЯ книга в `error(unreadable_chapter)` с именем файла (решение человека — не молча дропать); **E5** нет места → пред-проверка `disk_usage` vs `estimate_output_size` → `no_space` danger + cleanup ДО ffmpeg; **E10** недокопированный → дебаунс стабильности размера в скане (не армируется, пока копируется).
- **E15** «порог<главы» — корректный DEFER до слоя нарезки (P1).
- **Доказано Юркой:** `selfcheck_reliability` 16/16 (15 PASS+1 DEFER, 0 FAIL); полный гейт `selfcheck_all` **10/10** (216s) — E3/E5/E10 здоровую сборку не задели (m1/cover_pick/grouping/queue/cancel зелёные). Регрессия была лишь в одной тест-фикстуре (m0 собирал книгу с фейк-mp3 — E3 справедливо завернул; фикстуру починили на реальный mp3).

**Нарезка (P1) — движок ✅** (`split.py`: `plan_parts` жадная группировка глав по порогу `split_threshold_mb`=300, edge «глава>порога»→своя часть oversize; `split` stream-copy по рецепту §3 — `-ss/-to`, per-part FFMETADATA пере-база к 0, обложка, title «Часть N из M», `track=N/M`, **`-map_chapters 1`** против дублей; интеграция в `_real_build` при `split=True` → промежуточный полный файл удаляется, `result.parts`). **Доказано:** `selfcheck_split` 26/26 + Юрка вскрыл части ffprobe (Часть 1/5 «Глава первая» rebased-0, ОДНА глава без дублей, codec aac=stream-copy, mjpeg-обложка, title+track ок). `selfcheck_all` **11/11** (264с); E15 reliability DEFER→PASS. split=False не задет.
**UI-панель нарезки ✅** (`main.swift`: `GradientSlider` 100–700 МБ дефолт 300 + `split-preview` «≈ N частей по ~X МБ» + чипы `SplitPartChips` + oversize-предупреждение; `SplitPlanner` — порт `plan_parts` 1-в-1, парность доказана кросс-тестом A–D, пересчёт на слайдер+битрейт; `split_threshold_mb` едет в команду). **Юрка сверил вживую** (window-id, 12 глав/~1ГБ): toggle «Включена», слайдер 300 МБ, превью «≈ 4 части по ~259 МБ» + 4 чипа, оценка «≈ 1.04 ГБ · несколько .m4b» — чисто по spec §6. `selfcheck_all` **11/11**. (мелочь на потом: хедер длительности «12 глав · ~12 0 мин» — формат часов.)

## ✅ M1 ФУНКЦИОНАЛЬНО ЗАВЕРШЁН (все слои)
вертикаль · обложка (движок+пикер+выбор) · группировка · очередь · Status · отмена · надёжность(E1–E18) · нарезка(P1) + развязка self-check'ов. Гейт `selfcheck_all` **11/11** (~4 мин). Все слои сверены Юркой вживую (window-id) + боевые книги вскрыты ffprobe.

**Упаковка/DMG ✅** — `packaging/installer.sh` (детект ffmpeg+ffprobe; venv+Pillow для генерации обложек; копирование агента в App Support/bin; LaunchAgent `com.arrivarus.mp3tom4b.agent`, **WatchPaths=папка+`queue/commands`**, plutil-plist, идемпотентная загрузка; флаги `MP3TOM4B_NO_LAUNCHCTL`/`NO_VENV` для безопасного dry-run) + `packaging/applet.applescript` + `build/build-dmg.sh` (hdiutil). **`build/dist/mp3-to-m4b-0.1.0.dmg`** (3.0 МБ, ad-hoc подпись, без нотаризации — для личного теста, Gatekeeper→правый клик «Открыть»). **Юрка проверил БЕЗОПАСНО** (без изменений системы): `hdiutil verify` VALID, DMG монтируется (`.app`+Applications-симлинк, v0.1.0); dry-run установщика (test HOME, NO_LAUNCHCTL) — ffmpeg найден, plist валиден (plutil OK), WatchPaths верные, агент скопирован.

**Экран Setup (§01, 7-й из 7) ✅** — `SetupView.swift`: статус ffmpeg (найден/нет + `brew install ffmpeg`), выбор папки (`NSOpenPanel`, дефолт `~/Desktop/mp3-to-m4b`), кнопка «Установить» → `Process` запускает **забандленный** `Contents/Resources/installer.sh` (папка как argv[1]) → по успеху на Status. Детект первого запуска (`isAgentInstalled`: launchd-плист ИЛИ застейженный `bin/agent`). Юрка сверил вживую (window-id) состояние «ffmpeg найден» — чисто по мокапу 01. (Деталь: actionless-шестерёнка в шапке мокапа НЕ отрисована — правило «нет мёртвых кнопок»; добавить по запросу.) **DMG пересобран** — теперь ставится через окно без терминала. `selfcheck_all` 11/11; build-app/build-dmg exit 0.

## 🎉 M1 ПОЛНОСТЬЮ ЗАВЕРШЁН (все 7 экранов + все функции + GUI-установка)
**DMG для установки:** `build/dist/mp3-to-m4b-0.1.0.dmg` (3.4 МБ, ad-hoc, без нотаризации → Gatekeeper: правый клик «Открыть»). Установка: открыть DMG → перетащить в «Программы» → запустить → экран «Настройка» (ffmpeg + папка) → «Установить».

Осталось до релиза: **QA человеком на реальных книгах** (поставить DMG → ≥10 книг в Apple Books + сторонний плеер, test-plan §QA) · код-ревью (CodeReviewer ∥ Codex) → ⛔ **G5** (PR/релиз — СТОП, нужно «да», push необратим).
